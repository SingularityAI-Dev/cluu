---
context: phase
phase: 01-scaffold
task: 0
total_tasks: 8
status: planned_not_executed
last_updated: 2026-04-20T20:13:03.441Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Wizard-injected scaffolding files in the working tree** — The Vercel/PostHog wizard skill auto-wrote `instrumentation-client.ts`, `next.config.ts`, `src/lib/`, `posthog-setup-report.md` during the `gsd-planner` subagent runs (timestamps 21:51–22:08 on 2026-04-20). These are NOT from intentional execution — they are side effects of the `vercel-plugin` skill triggering on `@supabase/supabase-js` and `posthog` keywords inside planning-doc text. They partially implement what Plan 01 and Plan 06 are supposed to build. — **Structural mitigation**: Before running `/gsd:execute-phase 1`, delete the spurious files (recommended) OR manually reconcile them against Plans 01/06. Do NOT let the executor overwrite them without inspection — that produces a false success where the executor "completes" Plan 06 in one file-write because the file already exists with the right content.

**Do not proceed until the box above is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Wizard side-effects during planning | Vercel/PostHog wizard skill auto-writes runtime files (`instrumentation-client.ts`, `next.config.ts`, `src/lib/`, wizard reports) when planner subagents reference `@supabase/supabase-js` or `posthog` in plan text. Produces untracked files that look like execution artefacts but aren't. | blocking | Before executing any phase, run `git status --short` and verify zero untracked files exist outside `.planning/` and `docs/`. If any exist, trace their mtime to the most recent subagent run and delete. |
| Outdated Next.js middleware convention | Initial auto-planned Plan 01 contained "choose middleware.ts for compatibility with Supabase docs" — factually wrong as of April 2026. Next 16 renamed the convention to `proxy.ts` / `proxy`. Supabase SSR docs were updated to match. | advisory | CONTEXT.md D-25/D-26/D-27 locks the decision. Any regen must preserve these. Planner prompts should explicitly forbid the old convention by name. |
| Opus rate-limit mid-planner-run | Hit the Opus rate limit during the first planner run (21:51) — wrote 7/8 plans before 429. Plan 08 had to be hand-authored. | advisory | Budget Opus more conservatively during long multi-plan phases. Use Sonnet for low-risk plans (manual-smoke gates, documentation-only plans). |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 1 is **planned and verified but not executed**. All 8 PLAN.md files exist and have passed the plan-checker (0 BLOCKERs, 7 WARNINGs all resolved). No code has been produced by the executor yet. The Vercel/PostHog wizard did write some runtime files as a side-effect of planner skill injection — these are untracked and should be deleted before execution (see blocking constraint above).

Last commit: `d98f5f5 docs(state): record Phase 1 replanning complete`.

To resume: clean the working tree, collect external-service credentials, then run `/gsd:execute-phase 1`.
</current_state>

<completed_work>
## End-to-end GSD initialization (this session)

- Project initialized via `/gsd:new-project --auto @docs/cluu-v1-design.md`:
  - `.planning/PROJECT.md` — 15 active requirements, out-of-scope list, key decisions
  - `.planning/REQUIREMENTS.md` — 71 v1 REQ-IDs across 13 categories
  - `.planning/ROADMAP.md` — 5 coarse phases, all 71 reqs mapped
  - `.planning/STATE.md`, `.planning/config.json` (yolo/coarse/parallel/commit)
  - `CLAUDE.md` — project guide
- Research (4 parallel agents + synthesizer): STACK.md, FEATURES.md, ARCHITECTURE.md, PITFALLS.md, SUMMARY.md
- Design doc amendment (Next 14 → 16, Phaser 3.80 → 3.90) — recorded in `docs/cluu-v1-design.md` "Amendments" table + PROJECT.md
- Phase 1 CONTEXT.md via `/gsd:discuss-phase 1 --auto` — 24 D-XX decisions
- Phase 1 CONTEXT.md **revised** via interactive `/gsd:discuss-phase 1`:
  - D-25: root `proxy.ts` (not `middleware.ts`)
  - D-26: explicit cookie-sync to mutable `NextResponse` to avoid sign-out-loop
  - D-27: rename `lib/supabase/middleware.ts` → `lib/supabase/proxy.ts`; export `createProxyClient`
  - D-23 revised: Vitest `^2.1.8` → `^4.1`
- Plan 1 plans authored by Opus planner (8 plans, 3 waves); Plans 01+02 regenerated after the CONTEXT revision
- Plan-checker (Sonnet) verified all 8 plans — 0 BLOCKERs, 7 WARNINGs (cosmetic drift in Plans 04/06/07/08)
- 7 warnings resolved inline (s/middleware/proxy/ + task-shape note in Plan 08)

## Commits this session (10 total)
`061ad63` chore: add project config
`a925d2f` docs: initialize project
`144f950` docs(research): stack, features, architecture, pitfalls
`2e72dd8` docs(research): synthesize research summary
`29a89b0` docs: define v1 requirements
`9e02f9e` docs: create roadmap (5 phases)
`7b5627c` docs: amend design doc + PROJECT.md for Next.js 16 / Phaser 3.90
`1bcab86` docs(01): capture phase context
`6c3b89a` docs(01): add phase 1 plans (waves 1-3, 8 plans)
`af466e8` docs(state): record Phase 1 planning complete
`e7adb6d` docs(01): revise context for proxy.ts + Vitest 4
`d56a1f3` docs(state): record Phase 1 context revision
`1ad74c7` docs(01): regenerate Plans 01+02 for proxy.ts + Vitest 4
`7c77a90` docs(01): fix stale middleware refs in Plans 04/06/07/08 after proxy.ts revision
`d98f5f5` docs(state): record Phase 1 replanning complete
</completed_work>

<remaining_work>
## Phase 1 execution (not started)

**Wave 1 (parallelizable):**
- Plan 01 — Repo scaffold (fork `phaserjs/template-nextjs`, Next 16.2.x + React 19.2.x, pnpm workspace, Turbopack, Vitest ^4.1, Vercel deploy wiring)
- Plan 02 — Supabase schema + `proxy.ts` auth + RLS negative test + magic-link callbacks + cookie-sync pattern

**Wave 2 (depends on Wave 1):**
- Plan 03 — Zustand Store Factory + `/api/migrate-anonymous` idempotency + Vitest test (Pitfall 4)
- Plan 04 — Phaser mount via `next/dynamic ssr:false` + BootScene/MeadowScene/UIScene + destroy guard (Pitfall 5)
- Plan 05 — PlayerAnchor + Cluu 4-layer Container + tap-to-move + WASD + camera lerp

**Wave 3 (depends on Wave 2):**
- Plan 06 — Cookie-consent banner + consent-gated PostHog + Sentry (Pitfall 12)
- Plan 07 — Top-right settings menu with sign-out + first-visit anonymous session + auth-state migration trigger
- Plan 08 — **Manual cross-browser smoke gate** (`autonomous: false`) — you run magic-link flow on iOS Safari, Android Chrome, desktop Chrome/Firefox/Safari; fill `MANUAL-SMOKE.md`

## Phases 2–5 (after Phase 1 ships)

- **Phase 2 Walking Skeleton**: one encounter end-to-end (Sunflower), LOGIC.md compiler, Grading Gateway (2 LLM calls), 50-prompt grading test. **Project lives or dies here.**
- **Phase 3 Meadow + Library MD**: 6 more Meadow encounters, Cluu mood states + cosmetics (+Wardrobe UI), Library book + Markdown export (Core Value ships), first alpha (5-10 friends)
- **Phase 4 Biomes + Share Cards**: Village + Workshop + Tide Pools (scope-flex lever) + Library capstones, JSON + PNG exports, second alpha (20-50)
- **Phase 5 Launch Hardening**: 60fps iPhone SE 2020, bundle <2MB, onboarding redo, a11y + POPIA audits, public beta → launch
</remaining_work>

<decisions_made>
- Treated `docs/cluu-v1-design.md` as locked spec (user instruction at session start)
- Amended §8 of the design doc when GSD stack research found Next 14 EOL — recorded as "Amendment 2026-04-20" table in the doc itself
- Chose Next.js 16.2.x over 15.x (Recommended path, user-approved via AskUserQuestion)
- Coarse phase granularity (5 phases vs standard 7–8 or fine 10+) to match solo-build cadence
- Skipped per-phase research for Phase 1 because the roadmap flagged it and all canonical refs already live in `.planning/research/*.md` and CONTEXT.md
- Regenerated Plans 01+02 only (not full replan) after CONTEXT revision — matches user's explicit "regenerate only Plans 01 and 02" direction
- Fixed cosmetic drift in Plans 03–08 in-place rather than regenerating them; checker confirmed no functional changes, just prose terminology
- Flagged wizard-injected files as a blocking constraint rather than silently committing them
</decisions_made>

<blockers>
- **Wizard-injected working-tree files** (see Critical Anti-Patterns table above): resolution required before `/gsd:execute-phase 1` can run cleanly. Resolution path: `rm instrumentation-client.ts next.config.ts posthog-setup-report.md && rm -rf src/`. Do NOT delete `.claude/`, `.DS_Store`, or `assets/` — those have other reasons.
- **External service credentials** not yet collected: Vercel, Supabase (with anonymous sign-ins enabled in dashboard), PostHog EU, Sentry. Phase 1 executor will prompt via `user_setup` blocks but having them ready speeds execution.
</blockers>

## Required Reading (in order)

1. `.planning/phases/01-scaffold/01-CONTEXT.md` — 27 D-XX decisions, especially D-25/D-26/D-27 (proxy.ts + cookie-sync) and D-23 (Vitest ^4.1); canonical refs block
2. `.planning/phases/01-scaffold/01-01-repo-scaffold-PLAN.md` through `01-08-cross-browser-smoke-PLAN.md` — the actual executable plans; at minimum skim the `user_setup` blocks for env-var requirements
3. `.planning/research/STACK.md` — pinned versions + install block; "Integration gotchas §1" (proxy.ts), §2 (Supabase anonymous upgrade), §7 (Zustand Store Factory)
4. `.planning/research/ARCHITECTURE.md` — "Recommended Project Structure" (directory tree) + Patterns 1, 2, 3, 4, 5
5. `.planning/research/PITFALLS.md` — Pitfalls 4, 5, 12 (BLOCKERs that Phase 1 absorbs)
6. `docs/cluu-v1-design.md` — the locked spec; §8 has the amendment footer
7. `.planning/ROADMAP.md` — Phase 1 section (success criteria) + Phase 2–5 previews

## Critical Anti-Patterns (do NOT repeat these)

- **ANTI-PATTERN**: Silently committing working-tree files that look like execution output but are actually subagent side-effects → **Structural mitigation**: Before any execute-phase, run `git status --short`; untracked files outside `.planning/` and `docs/` must be traced to their origin before staging. (Prevention mechanism for the Wizard constraint at the top.)
- **ANTI-PATTERN**: Regenerating all plans when CONTEXT revision only affects a subset → **Structural mitigation**: Diff the CONTEXT revision against original decisions; only delete+regen plans whose decisions actually changed. Cosmetic cross-refs in sibling plans can be fixed in-place.
- **ANTI-PATTERN**: Using Next 14 `middleware.ts` convention under Next 16 → **Structural mitigation**: D-25/D-26/D-27 in CONTEXT.md lock the decision; plan-checker enforces via grep on `proxy.ts` + absence of `createMiddlewareClient`.

## Infrastructure State

- **Git**: initialized at session start, branch `master` (GSD did not auto-rename to `main`). Commit hash at pause: `d98f5f5`
- **Node**: system default; pnpm not yet installed project-locally (Plan 01 handles this)
- **Dev server**: not running
- **External services**: none provisioned yet
- **Vercel project**: not linked
- **Supabase project**: not created
- **Working tree dirt**: 9 untracked paths + 1 modified (`.planning/config.json`). See HANDOFF.json `uncommitted_files` for the full list and provenance.

## Pre-Execution Critique Required

Not applicable — plan-checker has already passed (0 BLOCKERs, 7 WARNINGs all resolved). The critical gate remaining is the human-action "clean working tree + collect credentials" step before `/gsd:execute-phase 1`.

<context>
The session began with an empty repo containing only `docs/cluu-v1-design.md` and `assets/`. Goal: take the design doc through the full GSD workflow (new-project → discuss-phase → plan-phase) and have Phase 1 ready to execute. That's what shipped.

One non-trivial discovery: the design doc's locked Next.js 14 pin is EOL. GSD stack research caught it and surfaced a CHECKPOINT before committing to it. User approved the Next 16 amendment; the design doc and PROJECT.md were updated to record it as an explicit "Amendments" table entry.

One mid-session correction: the first auto-run of `/gsd:discuss-phase 1 --auto` picked defaults that included the outdated `middleware.ts` guidance (even though research flagged the proxy convention). User caught it during interactive `/gsd:discuss-phase 1` and supplied concrete D-25/D-26/D-27 text. Plans 01+02 were deleted and regenerated. This correction pattern (auto → revise → targeted regen) worked well and preserved Plans 03–08 without re-burning Opus budget on them.

One meta-observation for a resuming agent: the wizard-injected files in the working tree are a known risk of running the Vercel plugin during planning. Watch for them after every planner run in future phases.
</context>

<next_action>
1. Read this file and check the blocking constraint checkbox only after understanding the Wizard Side-Effect issue.
2. Clean the working tree:
   ```bash
   rm instrumentation-client.ts next.config.ts posthog-setup-report.md
   rm -rf src/
   git status --short   # should leave only: .claude/, .DS_Store, .gitignore, assets/, M .planning/config.json
   ```
3. Decide on `.planning/config.json` modification — if it's just the `_auto_chain_active` flag toggling, reset to false and commit OR leave as-is.
4. Collect external-service credentials (Vercel, Supabase with anonymous sign-ins enabled, PostHog EU, Sentry). Ready them in a secure note.
5. Run `/gsd:execute-phase 1`.
6. When Plan 08 runs, drive the manual cross-browser smoke yourself (iOS Safari, Android Chrome, desktop Chrome/Firefox/Safari); fill `.planning/phases/01-scaffold/MANUAL-SMOKE.md`; commit.
7. After Phase 1 green, `/gsd:discuss-phase 2 --auto` to advance toward the Walking Skeleton encounter.
</next_action>
