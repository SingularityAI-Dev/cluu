---
phase: 01-scaffold
plan: 01
status: complete
completed_at: "2026-04-21"
---

# Plan 01-01 · Repo Scaffold — Summary

## Pinned versions that landed

| Package | Pinned | Verified |
|---|---|---|
| next | `16.2.4` | `pnpm list --depth=0` → `next 16.2.4` |
| react | `19.2.0` | `pnpm list --depth=0` → `react 19.2.0` |
| react-dom | `19.2.0` | `pnpm list --depth=0` → `react-dom 19.2.0` |
| phaser | `3.90.0` | `pnpm list --depth=0` → `phaser 3.90.0` (installed; not yet imported — Plan 04 mounts) |
| vitest | `^4.1.0` → `4.1.4` | `pnpm exec vitest --version` → `vitest/4.1.4` ✅ D-23 revised proof |
| @vitest/ui | `^4.1.0` → `4.1.4` | same |
| typescript | `^5.6.0` | strict config green |
| @biomejs/biome | `^1.9.0` | `pnpm lint` exits 0 |

## Green chain

- `pnpm install` — lockfile resolved cleanly (pnpm 9.15.0)
- `pnpm typecheck` — 0 errors
- `pnpm test` — 3/3 passing (`sanity.test.ts`, Vitest 4.1.4, jsdom active, Node 24+ asserted)
- `pnpm lint` — Biome checked 16 files, no fixes
- `pnpm build` — Next 16.2.4 with Turbopack compiled in 1136ms; static pages generated (`/`, `/_not-found`); TS check 1164ms

## Lint/format choice

Biome v1.9.0 (per "Claude's Discretion"). Single tool, single config. `biome.json` at repo root; `pnpm lint` / `pnpm lint:fix` / `pnpm format` scripts wired.

## Vercel link

Completed locally via Vercel CLI. `.vercel/project.json` present (gitignored) with project `cluu` under team `huboneintelligence-2022` (project id `prj_iUafjPILCC46QfSyQYv52lLHrU98`). `vercel.json` at repo root pins `framework: nextjs`, `regions: ["cpt1"]` (POPIA / Cape Town latency).

Vercel CLI version on disk is `50.41.0`; platform advertises `51.8.0`. Non-blocking — Plan 06 or Phase 5 hardening is the natural spot to bump the global install.

## Plan 02 ownership preserved

- `! test -f middleware.ts` ✅
- `! test -f proxy.ts` ✅ (Plan 02 creates `lib/supabase/proxy.ts` per D-25 / D-27)
- `! test -d lib/supabase` ✅
- `.env.example` carries Supabase placeholders with a Plan 02 pointer; no real values.

## Known deviations from plan

- None that affect success criteria. The `next-env.d.ts` file is autogenerated by Next on first `pnpm install`/build and is checked in as standard (gitignored content is auto-managed by Next itself).
- `supabase/` directory at repo root is NOT part of this plan — it was created by the user running `supabase init` + `supabase link` ahead of Plan 02. It is excluded from this plan's commit and will be incorporated by Plan 01-02.

## Pitfalls mitigated

- **Pitfall 5 #4** — `reactStrictMode: true` in `next.config.ts` (catches Phaser scene-leak bugs in dev).
- **D-23 revised** — Vitest `^4.1`, not `^2.x.y`. `vitest --version` reports `4.1.4`. No v2→v4 migration landmine waiting for Plans 03/05/06/07.
- **Next 14 EOL** — upgraded to 16.2.4 (Active LTS).

## Handoff

Plan 01-02 (Supabase auth) is unblocked:
- `.env.example` has the placeholder lines it will fill.
- `app/layout.tsx` is minimal and ready for Plan 03's `<StoreProvider>` + Plan 06's `<PostHogProvider>` / `<ConsentBanner />` wraps.
- User has already run `supabase init` + `supabase link` against the remote project (`xiccqasycfzodfgbcawh.supabase.co`) — Plan 02 can jump straight to proxy.ts + env wiring.
