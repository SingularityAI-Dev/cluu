---
phase: 01.1-visual-style-cluu-feel
plan: 04
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - game/scenes/BootScene.ts
  - game/entities/Cluu.ts
  - game/entities/Cluu.test.ts
  - game/config.ts
  - game/scenes/MeadowScene.ts
autonomous: true
requirements: []
tags: [phaser, aseprite-loader, idle-animation, tokens-integration]

must_haves:
  truths:
    - "Running pnpm dev and visiting /play renders production Cluu (chubby 4-legged Content pose) on production meadow tiles with no red missing-texture squares"
    - "Cluu idle-animates: breath loops continuously; blink fires every 5 to 8s; head_turn fires every 20 to 40s (D-16)"
    - "game/config.ts imports hexToInt + palette from @/lib/design/tokens — no raw '#e8f4d7' literal remains in that file"
    - "BootScene loads cluu_content via this.load.aseprite(...) — the old this.load.image('cluu_base', ...) call is removed"
    - "BootScene.create() calls this.anims.createFromAseprite('cluu_content') BEFORE starting MeadowScene"
    - "Cluu.ts constructs base sprite with texture key 'cluu_content' (renamed from 'cluu_base') and plays 'breath' animation"
    - "Sunflower before/after textures are preloaded (available for Phase 2 to consume)"
    - "Cluu.test.ts passes after the 'cluu_base' to 'cluu_content' rename"
    - "All existing Phase 1 tests still pass (no regression)"
  artifacts:
    - path: "game/scenes/BootScene.ts"
      provides: "Preloads cluu_content (Aseprite), grass_32, 4 blank cosmetic slots, Sunflower before/after; calls createFromAseprite in create()"
    - path: "game/entities/Cluu.ts"
      provides: "Cluu Container with 'cluu_content' base key plus scheduled breath/blink/head_turn idle animation"
    - path: "game/config.ts"
      provides: "Phaser config using hexToInt(palette.meadow_hint) for backgroundColor"
    - path: "game/entities/Cluu.test.ts"
      provides: "Updated tests referencing 'cluu_content' key; all pass"
  key_links:
    - from: "game/config.ts"
      to: "lib/design/tokens.ts"
      via: "import { palette, hexToInt } from @/lib/design/tokens"
    - from: "game/scenes/BootScene.ts"
      to: "public/sprites/cluu_content.png + .json"
      via: "this.load.aseprite('cluu_content', ...)"
    - from: "game/entities/Cluu.ts"
      to: "BootScene's Aseprite anim registration"
      via: "this.base.anims.play('breath')"
---

<objective>
Wire the production art (delivered in Plan 03) into Phaser. Switch BootScene's Cluu preload from `this.load.image('cluu_base', ...)` to `this.load.aseprite('cluu_content', ...)`. Call `this.anims.createFromAseprite('cluu_content')` in `BootScene.create()` so the three tagged animations (`breath`, `blink`, `head_turn`) are registered globally before MeadowScene starts. Update `Cluu.ts` to play `breath` as the infinite baseline and schedule `blink` + `head_turn` via `scene.time.delayedCall` with D-16 jitter ranges. Add the two Sunflower textures to BootScene.preload so Phase 2 can consume them immediately. Swap `game/config.ts`'s hardcoded `#e8f4d7` background for `hexToInt(palette.meadow_hint)` — the first live consumer of Plan 01's tokens module.

Purpose: This is where art delivery converts to running-game behaviour. After Plan 04, `pnpm dev` to `/play` shows the production Cluu breathing + occasionally blinking on production meadow tiles. All Phase 1 Cluu tests continue passing with the texture-key rename. The meadow scene background is tokens-sourced.

Output: 5 TypeScript files modified (BootScene, Cluu, Cluu.test, config, MeadowScene). Zero new files. All Wave-1 (tokens) + Wave-2 (art) artifacts consumed.
</objective>

<execution_context>
@/Users/rainierpotgieter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainierpotgieter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-visual-style-cluu-feel/01.1-CONTEXT.md
@.planning/phases/01.1-visual-style-cluu-feel/01.1-RESEARCH.md
@lib/design/tokens.ts
@docs/encounter-object-authoring.md

<interfaces>
Tokens module (Plan 01 output — now live):
- palette: `{ ui_paper, ink_900, accent_green, meadow_hint, cluu_body, ... } as const`
- spacing: `{ tile: 32, cluu: 48, encounter: 48 } as const`
- `hexToInt(hex: "#rrggbb"): number`

Art assets (Plan 03 output — now live under public/sprites/):
- public/sprites/cluu_content.png (Aseprite Packed spritesheet)
- public/sprites/cluu_content.json (JSON Hash with meta.frameTags: breath, blink, head_turn)
- public/sprites/grass_32.png (32x32 single-tile)
- public/sprites/encounter_meadow_sunflower_withered.png (48x48)
- public/sprites/encounter_meadow_sunflower_revived.png (48x48)

Phaser 3.90 APIs to use (verified in RESEARCH.md against installed .d.ts):
- this.load.aseprite(key, pngUrl, jsonUrl) — Phaser.Loader.LoaderPlugin (phaser.d.ts:66083)
- this.anims.createFromAseprite(key) — Phaser.Animations.AnimationManager (:1258)
- sprite.anims.play(key, ignoreIfPlaying?) — Phaser.GameObjects.Sprite (:36701)
- sprite.once('animationcomplete-KEY', fn) — standard per-anim event
- scene.time.delayedCall(ms, fn) — Phaser.Time.Clock
- Phaser.Math.Between(min, max) — integer range helper

Existing code contracts (must preserve):
From game/entities/Cluu.ts (current state):
- `export type CosmeticSlot = 'body' | 'head' | 'back' | 'eyes';`
- `export type CluuMood = 'stoked' | 'content' | 'sleepy' | 'blue';`
- `class Cluu extends Phaser.GameObjects.Container` with ctor(scene, x, y), follow(), setMood()/getMood(), setEquipped(), getChildCount() (test hook — DO NOT remove)

Test contract (must update 'cluu_base' to 'cluu_content' and keep everything else passing):
From game/entities/Cluu.test.ts (current):
- line 128: `'cluu_base',` — change to `'cluu_content',`
- line 173: `expect(scene.sprites[1].texture.key).toBe('cluu_base');` — change to `'cluu_content'`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: BootScene — swap Cluu image load for Aseprite loader; add Sunflower textures; register anims in create()</name>
  <files>game/scenes/BootScene.ts</files>
  <read_first>
    - game/scenes/BootScene.ts (current — loads cluu_base as single image)
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-RESEARCH.md section "Example 1: Complete BootScene preload with Aseprite"
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-CONTEXT.md (D-04 Sunflower preloaded here, D-16 idle anim tags, D-23 exit gate)
    - public/sprites/cluu_content.json (confirm tags exist via jq or node)
  </read_first>
  <action>
Replace the body of `game/scenes/BootScene.ts` with the following exact code (preserve leading `import * as Phaser from 'phaser';` + `bus` import):

File contents (keep imports, replace class body):

- class BootScene keeps `super({ key: 'BootScene' });` ctor
- preload() contains exactly these loads in this order:
  - `this.load.image('grass', '/sprites/grass_32.png');`
  - `this.load.aseprite('cluu_content', '/sprites/cluu_content.png', '/sprites/cluu_content.json');`
  - `this.load.image('cluu_body_pattern', '/sprites/cluu_body_pattern.png');`
  - `this.load.image('cluu_head', '/sprites/cluu_head.png');`
  - `this.load.image('cluu_eyes', '/sprites/cluu_eyes.png');`
  - `this.load.image('cluu_back', '/sprites/cluu_back.png');`
  - `this.load.image('encounter_meadow_sunflower_withered', '/sprites/encounter_meadow_sunflower_withered.png');`
  - `this.load.image('encounter_meadow_sunflower_revived', '/sprites/encounter_meadow_sunflower_revived.png');`
- create() contains in this exact order:
  1. `this.anims.createFromAseprite('cluu_content');` (MUST be first — Pitfall 6)
  2. `this.scene.start('MeadowScene');`
  3. `this.scene.launch('UIScene');`
  4. `bus.emit('game:ready', { timestampMs: Date.now() });`

Remove the existing `this.load.image('cluu_base', '/sprites/cluu_base.png');` line (file is deleted in Plan 03).

Update the file header comment block to reference D-16 idle animation + note that createFromAseprite registers breath/blink/head_turn globally.

Do NOT:
- Do NOT use `this.load.spritesheet({ frameWidth: 48, frameHeight: 48 })` — RESEARCH anti-patterns + D-17 lock to Aseprite-native loader.
- Do NOT hardcode frame indices via generateFrameNumbers — tag-driven via createFromAseprite.
- Do NOT move createFromAseprite to MeadowScene.create() — must run while Boot is active so Meadow spawns Cluu with anims already registered.
- Do NOT keep the 'cluu_base' key — rename is total (texture key + file name both).
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "this.load.aseprite('cluu_content'" game/scenes/BootScene.ts` exits 0
    - `grep -q "createFromAseprite('cluu_content')" game/scenes/BootScene.ts` exits 0
    - `grep -q "encounter_meadow_sunflower_withered" game/scenes/BootScene.ts` exits 0
    - `grep -q "encounter_meadow_sunflower_revived" game/scenes/BootScene.ts` exits 0
    - `grep -q "'cluu_base'" game/scenes/BootScene.ts` exits 1 (old key GONE)
    - `grep -q "cluu_base.png" game/scenes/BootScene.ts` exits 1 (old file path GONE)
    - `grep -n "createFromAseprite" game/scenes/BootScene.ts` returns a line number BEFORE the line number of `scene.start('MeadowScene')` (verified via awk or manual line compare — ordering per Pitfall 6)
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>
    BootScene preloads the production Cluu via Aseprite loader, preloads Sunflower before/after textures, and registers the three idle animations on the AnimationManager before MeadowScene takes over.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cluu entity — rename base texture key, play breath baseline, schedule blink + head_turn</name>
  <files>game/entities/Cluu.ts</files>
  <read_first>
    - game/entities/Cluu.ts (current — line 34 `'cluu_base'` literal)
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-RESEARCH.md section "Pattern 2: Idle animation state machine via scene.time.delayedCall" and "Example 2: Revised Cluu entity with idle animation"
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-CONTEXT.md (D-14 chubby 4-legged, D-15 Content pose, D-16 anim timing ranges)
  </read_first>
  <action>
Modify `game/entities/Cluu.ts`:

1. Rename the texture key on the base Sprite construction (currently line 34):
   - Before: `this.base = scene.add.sprite(0, 0, 'cluu_base');`
   - After: `this.base = scene.add.sprite(0, 0, 'cluu_content');`

2. After `body.setSize(40, 40);` and before the constructor's closing brace, add:
   - `this.base.anims.play('breath');` (start the infinite baseline loop)
   - `this.scheduleBlink(scene);`
   - `this.scheduleHeadTurn(scene);`

3. Add two private methods to the class (BEFORE the `getChildCount()` test hook so the test hook stays at the bottom):

   `private scheduleBlink(scene: Phaser.Scene): void` — body:
   - `const delay = Phaser.Math.Between(5000, 8000);` (D-16 range)
   - `scene.time.delayedCall(delay, () => { this.base.once('animationcomplete-blink', () => { this.base.anims.play('breath'); }); this.base.anims.play('blink'); this.scheduleBlink(scene); });`

   `private scheduleHeadTurn(scene: Phaser.Scene): void` — body:
   - `const delay = Phaser.Math.Between(20000, 40000);` (D-16 range)
   - `scene.time.delayedCall(delay, () => { this.base.once('animationcomplete-head_turn', () => { this.base.anims.play('breath'); }); this.base.anims.play('head_turn'); this.scheduleHeadTurn(scene); });`

4. Update the top-of-file comment: add "D-16: idle animation scheduled via scene.time.delayedCall — breath is the looping baseline; blink + head_turn pre-empt it on jittered intervals (RESEARCH Pattern 2)."

Do NOT:
- Do NOT add `setInterval` — RESEARCH "Don't Hand-Roll" flags this; `scene.time.delayedCall` survives scene shutdown cleanly.
- Do NOT `anims.play` on `this` (the Container) — only Sprites have anims. Play on `this.base` only (the Sprite child).
- Do NOT change the z-order or child-add order — the Phase 1 contract z=back/base/bodyPattern/head/eyes stays.
- Do NOT remove `getChildCount()` — it's a test hook, load-bearing for existing tests.
- Do NOT change the signature of `follow()`, `setMood()`, `getMood()`, `setEquipped()` — external callers rely on them.
- Do NOT play anims before the scene is ready — the BootScene runs createFromAseprite before MeadowScene starts, so at Cluu ctor time anims exist.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "'cluu_content'" game/entities/Cluu.ts` exits 0 (new key present)
    - `grep -q "'cluu_base'" game/entities/Cluu.ts` exits 1 (old key GONE — and the test file gets its own rename in Task 3)
    - `grep -q "this.base.anims.play('breath')" game/entities/Cluu.ts` exits 0
    - `grep -q "scheduleBlink" game/entities/Cluu.ts` exits 0
    - `grep -q "scheduleHeadTurn" game/entities/Cluu.ts` exits 0
    - `grep -q "Phaser.Math.Between(5000, 8000)" game/entities/Cluu.ts` exits 0 (D-16 blink range)
    - `grep -q "Phaser.Math.Between(20000, 40000)" game/entities/Cluu.ts` exits 0 (D-16 head_turn range)
    - `grep -q "animationcomplete-blink" game/entities/Cluu.ts` exits 0
    - `grep -q "animationcomplete-head_turn" game/entities/Cluu.ts` exits 0
    - `grep -q "scene.time.delayedCall" game/entities/Cluu.ts` exits 0
    - `grep -q "getChildCount" game/entities/Cluu.ts` exits 0 (test hook preserved)
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>
    Cluu.ts consumes the 'cluu_content' texture, plays breath on construction, and schedules blink + head_turn on jittered delayedCall timers within the D-16 ranges. The 4-layer Container z-order is unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Cluu.test.ts — rename 'cluu_base' to 'cluu_content' in test fixtures and assertions</name>
  <files>game/entities/Cluu.test.ts</files>
  <read_first>
    - game/entities/Cluu.test.ts (current — two references to 'cluu_base' at lines 128 + 173)
    - game/entities/Cluu.ts (post-Task-2 state — new 'cluu_content' key)
  </read_first>
  <action>
Edit `game/entities/Cluu.test.ts`:

1. Find every occurrence of the string literal `'cluu_base'` and replace with `'cluu_content'`.
   - Expected locations (per current file): line 128 (test fixture setup for texture manager), line 173 (assertion on sprites[1].texture.key).
   - If additional occurrences exist from prior refactors, rename them too.

2. Do NOT add new tests in this task — animation playback testing would require a Phaser animation-state mock that's overkill for a key-rename.

3. Run the test suite to confirm all Cluu tests pass after the rename.

Do NOT:
- Do NOT change the shape of the test fixtures — only rename the string literal.
- Do NOT add an animation-playback test — test infrastructure for Phaser anims in a pure-TS harness is heavy; the live `pnpm dev` smoke in Plan 05 covers animation verification.
- Do NOT remove or rename any existing test case.
  </action>
  <verify>
    <automated>pnpm test -- game/entities/Cluu.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'cluu_content'" game/entities/Cluu.test.ts` returns >= 2 (at minimum the two known sites renamed)
    - `grep -q "'cluu_base'" game/entities/Cluu.test.ts` exits 1 (old key GONE)
    - `pnpm test -- game/entities/Cluu.test.ts` exits 0 (all Cluu tests pass)
  </acceptance_criteria>
  <done>
    Cluu.test.ts references the renamed 'cluu_content' key; all Phase 1 Cluu tests continue to pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: game/config.ts — replace raw hex backgroundColor with hexToInt(palette.meadow_hint)</name>
  <files>game/config.ts</files>
  <read_first>
    - game/config.ts (current — line 14 `backgroundColor: '#e8f4d7'`)
    - lib/design/tokens.ts (palette.meadow_hint = '#e8f4d7', hexToInt helper)
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-RESEARCH.md section "Pitfall 5: Raw hex leak into Phaser scene code" (audit covers both '#' and '0x' forms)
  </read_first>
  <action>
Edit `game/config.ts`:

1. Add an import at the top (after the Phaser import):
   - `import { palette, hexToInt } from '@/lib/design/tokens';`
   - If the `@/*` alias is not configured in tsconfig, use a relative import: `import { palette, hexToInt } from '../lib/design/tokens';`
   - Verify the correct form by checking tsconfig.json before committing. Test by running pnpm typecheck.

2. Replace line 14:
   - Before: `backgroundColor: '#e8f4d7',`
   - After: `backgroundColor: hexToInt(palette.meadow_hint),`
   - Rationale: Phaser's GameConfig `backgroundColor` accepts string OR number; passing the integer form keeps game/config.ts token-referential and passes the Plan 05 audit which rejects both `#` and `0x` literals (per RESEARCH Pitfall 5).

3. Do not touch any other line in the file.

Do NOT:
- Do NOT write `backgroundColor: 0xe8f4d7` — Plan 05 audit regex rejects both `#` and `0x` literals outside tokens.ts.
- Do NOT introduce a separate Phaser-specific palette — tokens.ts is the single source.
- Do NOT add conditional imports — resolve the tsconfig path question once.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "hexToInt(palette.meadow_hint)" game/config.ts` exits 0
    - `grep -q "'#e8f4d7'" game/config.ts` exits 1 (raw hex GONE)
    - `grep -q "0xe8f4d7" game/config.ts` exits 1 (alternative leak form also absent)
    - `grep -qE "^import \\{ palette, hexToInt \\} from " game/config.ts` exits 0
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>
    game/config.ts is the first live consumer of lib/design/tokens.ts. The Phaser game background is set via hexToInt(palette.meadow_hint) — zero raw hex remains in the file.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: MeadowScene — replace raw hex scene background with tokens reference</name>
  <files>game/scenes/MeadowScene.ts</files>
  <read_first>
    - game/scenes/MeadowScene.ts (current — line 27 `this.cameras.main.setBackgroundColor(0xe8f4d7);`)
    - lib/design/tokens.ts (palette.meadow_hint + hexToInt)
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-RESEARCH.md section "Pitfall 5" (0x integer form must also be audit-clean)
  </read_first>
  <action>
Edit `game/scenes/MeadowScene.ts`:

1. Add an import at the top (after existing imports):
   - `import { palette, hexToInt } from '@/lib/design/tokens';`
   - (or relative path if alias isn't configured — match what you chose in Task 4)

2. Replace line 27:
   - Before: `this.cameras.main.setBackgroundColor(0xe8f4d7);`
   - After: `this.cameras.main.setBackgroundColor(hexToInt(palette.meadow_hint));`

3. Do not touch any other line in the file.

Do NOT:
- Do NOT add new scene logic — this task is surgical hex-removal only.
- Do NOT refactor the tile-loop — tile variant support is deferred per CONTEXT Open Question 2.
- Do NOT change the 24x18 grid dimensions or tile size literals (32). (Note: the literal `32` at line 20 is a `const tileSize = 32;` — it's dimensional, not a colour; the palette audit regex is anchored to `#` and `0x[a-f]` patterns, so the integer 32 does not trigger it. Leave it alone.)
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "hexToInt(palette.meadow_hint)" game/scenes/MeadowScene.ts` exits 0
    - `grep -q "0xe8f4d7" game/scenes/MeadowScene.ts` exits 1 (raw 0x literal GONE)
    - `grep -qE "^import \\{ palette, hexToInt \\} from " game/scenes/MeadowScene.ts` exits 0
    - `pnpm typecheck` exits 0
    - `pnpm test` still passes (no Phase 1 regression)
  </acceptance_criteria>
  <done>
    MeadowScene sets its camera background via hexToInt(palette.meadow_hint). Combined with Task 4, both scene-layer and game-config-layer background sources are tokens-referential. The Plan 05 audit will not flag game/config.ts or game/scenes/MeadowScene.ts for raw-hex leakage.
  </done>
</task>

</tasks>

<threat_model>
**Input surfaces:** none (no user input added; all changes are internal Phaser wiring and test fixtures)
**Data surfaces:** static texture keys + animation tag names consumed from committed assets
**Threats:** none identified — texture-key rename and tokens-referential refactor cannot introduce an exploitable surface
**Mitigations:** N/A
**Block-on severity:** high (per default `security_enforcement` enabled) — this plan has no threats to block on
</threat_model>

<verification>
- `pnpm typecheck` exits 0
- `pnpm test` exits 0 (all Phase 1 tests still pass after 'cluu_base' to 'cluu_content' rename)
- `grep -rE "'cluu_base'|cluu_base\\.png" game/` returns no matches (total rename complete)
- `grep -rE "#e8f4d7|0xe8f4d7" game/` returns no matches in .ts files (Pitfall 5 prevention; Plan 05 audit confirms globally)
- `grep -q "this.load.aseprite('cluu_content'" game/scenes/BootScene.ts` exits 0
- `grep -q "createFromAseprite('cluu_content')" game/scenes/BootScene.ts` exits 0
- Line ordering in BootScene.create(): `createFromAseprite` appears BEFORE `scene.start('MeadowScene')`
- Manual: `pnpm dev` and visit /play — Cluu renders (not a red square), breath animation visible, anchor-follow behaviour unchanged from Phase 1
</verification>

<success_criteria>
- BootScene loads cluu_content as Aseprite spritesheet + JSON Hash and registers anims in create()
- Cluu plays breath continuously; blink and head_turn fire on D-16 jittered schedules
- game/config.ts + MeadowScene.ts source their background colour from lib/design/tokens.ts (no raw hex)
- Cluu.test.ts renamed; all pre-existing tests pass
- Running `pnpm dev` shows production Cluu + production meadow tiles + no console errors; Phase 1 anchor-follow regression-free
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-visual-style-cluu-feel/01.1-04-SUMMARY.md` with: list of 5 files modified, confirmation of 'cluu_base' → 'cluu_content' rename being complete (grep proof), confirmation of tokens import in config.ts + MeadowScene.ts, pnpm typecheck + pnpm test results, and a one-line note that Plan 05 can now run the global palette audit against the post-refactor codebase.
</output>
