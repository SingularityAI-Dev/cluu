---
phase: 01.1-visual-style-cluu-feel
plan: 01
subsystem: design-system
tags: [design-tokens, palette, typescript, phaser, as-const]

# Dependency graph
requires:
  - phase: 01-scaffold
    provides: "Phase 1 inline-palette hex literals (#faf8f3 / #2d2a26 / #2d6a4f / #e8f4d7 / chrome grays) that must be preserved verbatim"
provides:
  - "lib/design/tokens.ts — canonical palette + spacing + radii + hexToInt"
  - "14 palette tokens: 5 cross-biome cores, 6-step ink/chrome scale, D-09 Meadow triad"
  - "hexToInt() helper — framework-agnostic, handles #rrggbb and #rgb shorthand"
  - "PaletteToken + PaletteHex type aliases for downstream consumers"
affects: [01.1-02, 01.1-03, 01.1-04, 01.1-05, 05-launch-hardening]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TS `as const` literal objects for design tokens (typeof-narrowing over enum)"
    - "Role-based token naming (meadow_grass_shadow) over pigment (green900)"
    - "Framework-agnostic palette module — no phaser import; consumers convert via hexToInt"

key-files:
  created:
    - "lib/design/tokens.ts — canonical palette + spacing + radii + hexToInt helper"
  modified: []

key-decisions:
  - "Preserve every Phase 1 inline hex as a named token so Phase 5 overlay restyle is a pure import swap (D-05 defer)"
  - "Token names are role-based not pigment-based — meadow_grass_shadow, not green900"
  - "hexToInt expands #rgb shorthand to #rrggbb before parseInt — supports both input forms"
  - "No phaser import in tokens.ts — keeps module consumable from Vitest + future non-Phaser contexts"

patterns-established:
  - "Pattern 1: `as const` object + `keyof typeof` type aliases for all design-token tables"
  - "Pattern 2: framework-agnostic token module with helper functions (hexToInt) for framework-specific conversions"

requirements-completed: []  # Plan has no REQ-IDs in frontmatter (INSERTED quality phase)

# Metrics
duration: ~15min
completed: 2026-04-21
---

# Phase 01.1 Plan 01: Design Tokens Summary

**Canonical palette + spacing + radii module (lib/design/tokens.ts) with 14 role-named colour tokens, D-09 Meadow triad byte-exact, D-10 Cluu warm-cream-peach body, and a framework-agnostic hexToInt helper — unblocks Plan 04 (scene consumer) and Plan 05 (palette-audit anchor).**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-21T15:55Z (approx)
- **Completed:** 2026-04-21T16:10:01Z
- **Tasks:** 1 (Task 1: Create lib/design/tokens.ts)
- **Files modified:** 1 (created)

## Accomplishments

- Single-file canonical design-token module at `lib/design/tokens.ts`
- Every Phase 1 inline-style hex literal preserved verbatim as a named token (D-05 defer honoured — zero regression risk for future overlay restyle)
- D-09 Meadow triad shipped byte-exact (`#6fa872` / `#9bc49e` / `#42744b`)
- D-10 Cluu body family shipped byte-exact (`#f4d1b5`)
- `hexToInt()` helper for Phaser colour APIs — handles both `#rrggbb` and `#rgb` shorthand
- Framework-agnostic — no phaser import, safe to consume from Vitest tests (Plan 05) and future non-Phaser contexts

## Task Commits

Each task was committed atomically:

1. **Task 1: Create lib/design/tokens.ts with palette + spacing + radii + hexToInt helper** — `a6064e5` (feat)

## Files Created/Modified

- `lib/design/tokens.ts` — canonical design tokens: `palette` (14 keys), `spacing` (3 keys), `radii` (3 keys), `hexToInt()` helper, `PaletteToken` + `PaletteHex` type aliases.

### Palette keys shipped (14 total)

Cross-biome cores (5):
- `ui_paper: '#faf8f3'` (preserves app/globals.css body bg)
- `ink_900: '#2d2a26'` (preserves app/globals.css body color)
- `accent_green: '#2d6a4f'` (preserves globals.css anchor + signin CTA + global-error retry)
- `meadow_hint: '#e8f4d7'` (preserves game/config.ts scene bg + CanvasSkeleton)
- `cluu_body: '#f4d1b5'` — **D-10 locked family, byte-exact**

Ink / chrome scale (6):
- `ink_700: '#4a5240'` (CanvasSkeleton text)
- `ink_600: '#555555'` (signin subtitle)
- `ink_500: '#666666'` (landing helper + error copy)
- `ink_400: '#777777'` (signin helper)
- `white: '#ffffff'` (CTA text + card backgrounds)
- `stone_200: '#d9d6ce'` (input + menu borders)

Meadow biome triad (3) — **D-09 locked, byte-exact**:
- `meadow_grass_mid: '#6fa872'`
- `meadow_grass_highlight: '#9bc49e'`
- `meadow_grass_shadow: '#42744b'`

### Spacing (D-12)

- `tile: 32` (Phase 1 D-10 lock)
- `cluu: 48` (Phase 1 D-13 lock)
- `encounter: 48` (D-21 default)

### Radii (D-12, forward-looking)

- `sm: 4`, `md: 8`, `lg: 16` — no Phase 01.1 consumer, shipped so Phase 5 overlay restyle is one pass.

## Verification Results

- `tsc --noEmit` exits 0 (ran via parent `node_modules/.bin/tsc` — worktree has no local node_modules, but TS compiles against shared install)
- All 16 plan-mandated grep checks PASS (each locked hex present; `export function hexToInt`, `export const spacing`, `export const radii`, `export type PaletteToken` all present)
- `grep -c "as const"` = 5 (≥3 required — palette, spacing, radii each marked `as const`; two extra occurrences in JSDoc / type extractions are incidental)
- No `from 'phaser'` import present
- No new dependencies added to `package.json`
- Ad-hoc evaluation: `hexToInt('#e8f4d7')` = 15267031 = `0xe8f4d7` ✓; `hexToInt('#fff')` = 16777215 = `0xffffff` ✓ (JSDoc example corrected to match actual integer values)

## Decisions Made

- **Followed plan as specified.** One minor JSDoc correction during verification: the original example comment showed `hexToInt(palette.meadow_hint) // => 0xe8f4d7 = 15266519` (as suggested by plan); actual integer value is `15267031`. Updated JSDoc to reflect the true value so the comment is not misleading. This is a docstring clarification, not a behavioural deviation.

## Deviations from Plan

None — plan executed exactly as written, with a single JSDoc clarity fix caught during self-verification (described above under "Decisions Made").

## Issues Encountered

- **Worktree branch base mismatch (fixed pre-execution).** Worktree HEAD was at `10c7bb0` (a `main`-lineage commit), not the target phase-planning commit `9f58ede`. Per the SubagentStart `<worktree_branch_check>` protocol, reset HEAD to the correct base before touching files. Working tree was clean; no work lost. After reset, `git log --oneline -1` shows `9f58ede docs(state): phase 01.1 planned — 5 plans ready to execute`.

- **Local `tsc` binary absent in worktree node_modules.** Worktree does not have its own `node_modules/` (shared install lives at parent `/Users/rainierpotgieter/development/cluu/node_modules/`). Ran typecheck via the parent's `node_modules/.bin/tsc --noEmit` against the worktree's `tsconfig.json`. Exit code 0 confirms strict-mode compilation. Not a plan deviation — just a worktree-tooling detail.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Plan 04 ready:** `game/config.ts` can now `import { palette, hexToInt } from '@/lib/design/tokens'` and replace the literal `'#e8f4d7'` with `hexToInt(palette.meadow_hint)`.
- **Plan 05 ready:** palette-audit test can allowlist `lib/design/tokens.ts` as the single approved home for hex literals, and can ALLOWLIST the Phase 1 overlay files per D-05 defer (`app/globals.css`, `app/global-error.tsx`, `app/page.tsx`, `app/auth/signin/page.tsx`, `app/play/CanvasSkeleton.tsx`, `ui/ConsentBanner.tsx`, `ui/SettingsMenu.tsx`).
- **No blockers.** Other Wave 1 plans may execute in parallel; Wave 2 consumers (Plan 04 scene integration) can proceed as soon as their executor picks up.

## Self-Check

- `[FOUND] lib/design/tokens.ts` — verified present, 117 lines, all required exports + hex values
- `[FOUND] a6064e5` — commit present in `git log --oneline` (feat(01.1-01): add canonical design tokens …)

## Self-Check: PASSED

---
*Phase: 01.1-visual-style-cluu-feel*
*Completed: 2026-04-21*
