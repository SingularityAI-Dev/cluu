---
phase: 01.1-visual-style-cluu-feel
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - public/sprites/cluu_content.png
  - public/sprites/cluu_content.json
  - public/sprites/grass_32.png
  - public/sprites/encounter_meadow_sunflower_withered.png
  - public/sprites/encounter_meadow_sunflower_revived.png
  - design/aseprite/cluu_content.ase
  - design/aseprite/meadow_grass.ase
  - design/aseprite/encounter_meadow_sunflower.ase
  - design/style-guide.png
  - public/sprites/cluu_base.png
autonomous: false
requirements: []
tags: [art-assets, human-in-the-loop, aseprite, sprites]

must_haves:
  truths:
    - "User delivers production Cluu Content-pose spritesheet (48×48 × N frames) exported from Aseprite with ≥ 1 frameTag"
    - "User delivers production meadow grass tile (32×32, single tile — variant support deferred per CONTEXT Open Question 2)"
    - "User delivers Withered Sunflower before + after PNGs (48×48 each, same silhouette)"
    - "User delivers design/style-guide.png documenting palette + Cluu reference + tiles + Sunflower"
    - "All PNGs use ONLY colours that exist in lib/design/tokens.ts palette (Plan 01) — verified manually against style guide"
    - "Aseprite source files (.ase) committed under design/aseprite/ (D-20)"
    - "No .ase files accidentally land in public/ (Pitfall 3)"
    - "The placeholder file public/sprites/cluu_base.png is deleted — the production file is cluu_content.png (rename per RESEARCH §Rename decision)"
  artifacts:
    - path: "public/sprites/cluu_content.png"
      provides: "Production Cluu Content-pose spritesheet (Aseprite Packed layout) — 48×48 per frame, ≥ 2 frames for breath + ≥ 3 blink + ≥ 3 head_turn"
      contains: "Aseprite Packed layout frames"
    - path: "public/sprites/cluu_content.json"
      provides: "Aseprite JSON Hash with meta.frameTags for 'breath', 'blink', 'head_turn'"
    - path: "public/sprites/grass_32.png"
      provides: "Production Meadow grass tile — 32×32, uses palette.meadow_grass_* triad"
    - path: "public/sprites/encounter_meadow_sunflower_withered.png"
      provides: "Sunflower before-state sprite — 48×48, Phase 2 Describe encounter asset"
    - path: "public/sprites/encounter_meadow_sunflower_revived.png"
      provides: "Sunflower after-state sprite — 48×48, same silhouette as withered"
    - path: "design/aseprite/cluu_content.ase"
      provides: "Layered Aseprite source for the Cluu Content pose — future-editable"
    - path: "design/aseprite/meadow_grass.ase"
      provides: "Aseprite source for the grass tile"
    - path: "design/aseprite/encounter_meadow_sunflower.ase"
      provides: "Aseprite source for Sunflower before/after (2 frames or 2 layers)"
    - path: "design/style-guide.png"
      provides: "§16 Visual style guide — single PNG with palette swatches, Cluu reference, tile samples, Sunflower before/after"
  key_links:
    - from: "public/sprites/cluu_content.png"
      to: "public/sprites/cluu_content.json"
      via: "Aseprite export — JSON references the PNG sheet"
      pattern: "cluu_content\\.png"
    - from: "public/sprites/cluu_content.json"
      to: "Phaser createFromAseprite (Plan 04 consumer)"
      via: "meta.frameTags array drives anim names"
      pattern: '"frameTags"'
---

<objective>
Human-in-the-loop delivery of all production art assets for Phase 01.1. The user produces the PNGs + `.ase` sources following the authoring spec (Plan 02) and style-guide targets; each sub-task has explicit delivery requirements the user satisfies manually. Claude does not generate art — plans verify file-exists + dimensions + metadata structure per D-18 / D-19.

Purpose: Phase 1 ships with 88-byte placeholder PNGs; Phase 2 cannot begin encounter work without the Sunflower before/after sprites; Plan 04 cannot wire the idle animation without a real Aseprite spritesheet + JSON. This plan is the single concentrated art-production block — everything else in the phase hinges on these assets.

Output: 5 production PNGs (4 in `public/sprites/`, 1 in `design/`), 1 Aseprite JSON Hash file, 3 `.ase` sources. The existing placeholder `cluu_base.png` is deleted; `cluu_content.png` (+ `.json`) replaces it under a renamed texture key (Plan 04 updates the code refs).
</objective>

<execution_context>
@/Users/rainierpotgieter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainierpotgieter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01.1-visual-style-cluu-feel/01.1-CONTEXT.md
@.planning/phases/01.1-visual-style-cluu-feel/01.1-RESEARCH.md
@docs/encounter-object-authoring.md
@lib/design/tokens.ts
@docs/cluu-v1-design.md

<interfaces>
<!-- Production pipeline (D-18): User prompts an AI image model of their choice for pixel-art
     candidates, picks the best fit, cleans up in Aseprite to hit the locked palette and pixel
     discipline. Plans do NOT call image APIs. -->

<!-- Target palette tokens (from lib/design/tokens.ts, Plan 01):
     palette.cluu_body          = '#f4d1b5'  (D-10 warm cream/peach — Cluu's base body)
     palette.meadow_grass_mid   = '#6fa872'  (D-09)
     palette.meadow_grass_highlight = '#9bc49e'  (D-09)
     palette.meadow_grass_shadow    = '#42744b'  (D-09)
     palette.ink_900            = '#2d2a26'  (outlines, pupils)
     palette.ui_paper           = '#faf8f3'  (bg eyes / whites)
  
  Cluu MAY introduce minor accent hues (e.g., ear-pink, paw-pad) that get ADDED to tokens.ts
  as part of this plan's acceptance — any new hue must be written into lib/design/tokens.ts
  with a role-based name. Same rule for Sunflower petal yellow and stem greens.
-->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User delivers Cluu Content-pose production spritesheet + Aseprite source</name>
  <files>
    public/sprites/cluu_content.png
    public/sprites/cluu_content.json
    design/aseprite/cluu_content.ase
    public/sprites/cluu_base.png (DELETE — replaced by cluu_content)
  </files>
  <read_first>
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-CONTEXT.md §decisions (D-06 chunky pixel art, D-07 loose pixel discipline, D-14 chubby 4-legged, D-15 head-tilt + bright eyes, D-16 idle animation tags, D-17 sprite-frame animation, D-18 AI + Aseprite cleanup, D-20 commit .ase)
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-RESEARCH.md §"Pattern 1: Aseprite-native sprite loading" (export settings), §"Pitfall 1" (tags checkbox), §"Pitfall 3" (no .ase in public/), §"Pitfall 4" (author at 1:1), §"Open Question 3" (optional 4 hidden cosmetic layers)
    - docs/encounter-object-authoring.md (Plan 02 — authoring spec; Cluu follows same Aseprite export rules)
    - lib/design/tokens.ts (Plan 01 — palette.cluu_body is the target, #f4d1b5; any new accent hues added here get written back to tokens.ts)
  </read_first>
  <action>
    **Deliverable:** A production Cluu sprite + idle animation, exported via Aseprite's Packed spritesheet + JSON Hash workflow.

    **What to produce:**
    - **Source file:** `design/aseprite/cluu_content.ase` — Aseprite document, 48×48 canvas.
      - At least 3 tagged animations in the timeline:
        - `breath` — 2–3 frames, loop direction forward, ~500ms per frame (≈1.5s cycle, D-16).
        - `blink` — 3 frames, direction forward, no loop.
        - `head_turn` — 3–5 frames, direction forward, no loop.
      - Content pose (D-15): chubby 4-legged silhouette, head slightly tilted, bright neutral eyes, no mouth or very subtle mouth that reads clean at 48×48.
      - Legs part of the base sprite (not a cosmetic slot — D-14 literal).
      - Back area slightly visible behind body so future cape cosmetic (Phase 3) peeks out — don't fully tuck the back behind the body.
      - RECOMMENDED (RESEARCH §Open Question 3): include 4 empty hidden layers named `back`, `body_pattern`, `head`, `eyes` in the .ase for Phase 3 alignment reference. Zero runtime cost (Packed export drops empty layers).

    - **Export:** Run Aseprite's `File → Export Sprite Sheet` with these exact settings (from Plan 02 / RESEARCH §Pattern 1):
      1. Layout: Sheet type = **Packed**, Constraints = **None**, **Merge Duplicates** checked.
      2. Sprite: Layers = **Visible layers**, Frames = **All frames**.
      3. Borders: **Trim Sprite** + **Trim Cells** checked; Border Padding / Spacing / Inner Padding ≥ 1.
      4. Output: **Output File** = `public/sprites/cluu_content.png`; **JSON Data** = `public/sprites/cluu_content.json`, format **Hash** (or Array — Phaser accepts both); **Tags** checkbox CHECKED (Pitfall 1); **Item Filename** = `{frame}`.

    - **Palette discipline:**
      - Primary body colour = `palette.cluu_body` = `#f4d1b5` (D-10 locked family; exact hex is the starting point — if Aseprite cleanup produces a warmer/cooler shade, UPDATE `lib/design/tokens.ts` to match AND confirm the Meadow biome pair still reads).
      - Outlines = `palette.ink_900` (`#2d2a26`).
      - Eye whites = `palette.ui_paper` (`#faf8f3`) or a warm-off-white — if off-white, add as `palette.eye_white` token.
      - Any NEW hue (ear-pink, paw-pad, tiny-cheek-blush, etc.) MUST be added to `lib/design/tokens.ts` as a role-named token (`palette.cluu_ear_pink`) with a value between `#e8b8a0` and `#e8c0c0` etc. — exact hex your call.

    - **Delete:** Remove the Phase 1 placeholder `public/sprites/cluu_base.png`. Plan 04 renames the Phaser texture key from `cluu_base` to `cluu_content`.

    - **Commit:** `git add design/aseprite/cluu_content.ase public/sprites/cluu_content.png public/sprites/cluu_content.json && git rm public/sprites/cluu_base.png`

    **Verification before signalling complete:**
    - [ ] `file public/sprites/cluu_content.png` reports "PNG image data, ___ x ___" with dimensions consistent with Packed Aseprite layout (typically wider than 48 — that's expected).
    - [ ] `cat public/sprites/cluu_content.json | jq '.meta.frameTags | length'` returns ≥ 3.
    - [ ] `cat public/sprites/cluu_content.json | jq '.meta.frameTags[].name'` includes `"breath"`, `"blink"`, `"head_turn"`.
    - [ ] `test -f design/aseprite/cluu_content.ase` and file size > 500 bytes.
    - [ ] `test ! -f public/sprites/cluu_base.png` (placeholder deleted).
    - [ ] `find public -name '*.ase'` returns NOTHING (Pitfall 3 — .ase never in public/).
    - [ ] Visual QA: open the PNG, confirm pose reads as "content, listening, tilted head".

    **If Aseprite is not installed:** install locally ($19.99 one-time from aseprite.org) before executing. Phase stalls without it per CONTEXT D-20.
  </action>
  <verify>
    <automated>test -f public/sprites/cluu_content.png &amp;&amp; test -f public/sprites/cluu_content.json &amp;&amp; test -f design/aseprite/cluu_content.ase &amp;&amp; test ! -f public/sprites/cluu_base.png &amp;&amp; [ $(find public -name '*.ase' | wc -l) -eq 0 ] &amp;&amp; [ $(cat public/sprites/cluu_content.json | node -e 'const j=JSON.parse(require("fs").readFileSync(0,"utf8"));process.stdout.write(String((j.meta?.frameTags||[]).length))') -ge 3 ] &amp;&amp; cat public/sprites/cluu_content.json | node -e 'const j=JSON.parse(require("fs").readFileSync(0,"utf8"));const tags=(j.meta?.frameTags||[]).map(t=>t.name);process.exit(tags.includes("breath")&amp;&amp;tags.includes("blink")&amp;&amp;tags.includes("head_turn")?0:1)'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f public/sprites/cluu_content.png` exits 0 and file size > 1000 bytes (real PNG, not placeholder)
    - `test -f public/sprites/cluu_content.json` exits 0 and file size > 500 bytes
    - `test -f design/aseprite/cluu_content.ase` exits 0 and file size > 500 bytes (Aseprite binary)
    - `test ! -f public/sprites/cluu_base.png` (placeholder deleted)
    - `find public -name '*.ase' | wc -l` returns 0 (Pitfall 3 — no .ase leak into public/)
    - `cat public/sprites/cluu_content.json | node -e '...'` reports `meta.frameTags.length >= 3`
    - `meta.frameTags` contains names `breath`, `blink`, `head_turn` (exact match, lowercase)
    - If new accent tokens were added to `lib/design/tokens.ts`, `pnpm typecheck` still passes
    - Aseprite source committed at `design/aseprite/cluu_content.ase`
    - Visual QA self-confirmed against D-14 (chubby 4-legged) + D-15 (head tilt + bright eyes) (manual)
  </acceptance_criteria>
  <done>
    Production Cluu Content pose PNG + JSON Hash + .ase source committed. `cluu_base.png` placeholder deleted. Plan 04 can now `this.load.aseprite('cluu_content', ...)` and `this.anims.createFromAseprite('cluu_content')` will create the three tagged animations.
  </done>
  <resume-signal>Type "cluu-delivered" when the Cluu art is committed and all verifications pass, or describe blockers (e.g., "Aseprite not installed yet").</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User delivers production Meadow grass tile + Aseprite source</name>
  <files>
    public/sprites/grass_32.png
    design/aseprite/meadow_grass.ase
  </files>
  <read_first>
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-CONTEXT.md §decisions (D-06 chunky pixel art, D-09 Meadow triad locked)
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-RESEARCH.md §"Open Question 2" (tile variant support — ship with one tile)
    - docs/encounter-object-authoring.md (tile-grid alignment rules)
    - lib/design/tokens.ts (palette.meadow_grass_mid/highlight/shadow — these 3 hex values define the tile)
  </read_first>
  <action>
    **Deliverable:** Production meadow grass tile at 32×32, single variant (variant support deferred per CONTEXT Open Question 2).

    **What to produce:**
    - **Source:** `design/aseprite/meadow_grass.ase`, 32×32 canvas.
      - Uses EXACTLY the three locked Meadow triad hex values from `lib/design/tokens.ts`:
        - `palette.meadow_grass_mid` = `#6fa872` (dominant)
        - `palette.meadow_grass_highlight` = `#9bc49e` (grass blade tips, small highlights)
        - `palette.meadow_grass_shadow` = `#42744b` (base of blades, clumps)
      - Visual target: "afternoon grass" — warm cozy, saturated, reads clean as a tiled pattern when 24×18 tiles fill a scene.
      - No outline around the tile (tiles butt-edge; an outline makes a visible grid — anti-pattern).
      - Tileable: the right edge should blend into the left edge of the next tile, same for top/bottom. Simple test: place 2×2 copies in Aseprite; no visible seams.

    - **Export:** `File → Export Sprite Sheet` with settings from Plan 02 (Packed, Merge Duplicates) OR simply export the canvas as PNG via `File → Export` — single-frame tile doesn't need sheet metadata. Target: `public/sprites/grass_32.png`, exactly 32×32.

    - **Commit:** `git add design/aseprite/meadow_grass.ase public/sprites/grass_32.png`

    **Verification before signalling complete:**
    - [ ] `file public/sprites/grass_32.png` reports "PNG image data, 32 x 32" (exact dimensions).
    - [ ] `test -f design/aseprite/meadow_grass.ase`.
    - [ ] Visual QA: open the PNG, confirm it reads as grass (not a solid colour), and confirm the three triad hex values are the only visible pigments (Aseprite's "Preview Palette" view is the fastest check).

    **Variant decision (deferred per CONTEXT Open Question 2):** If AI generation / cleanup produces 2–3 variants "for free" and the user wants to ship them, this is an acceptable scope expansion: add `grass_32_b.png` / `grass_32_c.png`, commit matching `.ase` files, and note in Plan 04 that `MeadowScene` will random-select. Otherwise ship with one tile — the dominant default.
  </action>
  <verify>
    <automated>test -f public/sprites/grass_32.png &amp;&amp; test -f design/aseprite/meadow_grass.ase &amp;&amp; (file public/sprites/grass_32.png | grep -qE '32 x 32|32x32')</automated>
  </verify>
  <acceptance_criteria>
    - `test -f public/sprites/grass_32.png` exits 0 and file size > 200 bytes (real PNG, not placeholder which is 180 bytes)
    - `file public/sprites/grass_32.png` reports `32 x 32` dimensions
    - `test -f design/aseprite/meadow_grass.ase` exits 0 and size > 500 bytes
    - Visual QA self-confirmed: reads as grass, uses only the D-09 Meadow triad (plus ink outline hues if any — those would need to be added to tokens.ts as `palette.meadow_grass_outline` if introduced)
  </acceptance_criteria>
  <done>
    Production meadow tile PNG + .ase source committed. `MeadowScene` already loads via `this.load.image('grass', '/sprites/grass_32.png')` — no code change needed (Plan 04 only changes the scene background colour reference via tokens).
  </done>
  <resume-signal>Type "grass-delivered" when the grass tile is committed, or describe blockers.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: User delivers Withered Sunflower before + after PNGs + Aseprite source</name>
  <files>
    public/sprites/encounter_meadow_sunflower_withered.png
    public/sprites/encounter_meadow_sunflower_revived.png
    design/aseprite/encounter_meadow_sunflower.ase
  </files>
  <read_first>
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-CONTEXT.md §decisions (D-04 Sunflower before/after produced here, D-21 encounter-object spec)
    - docs/encounter-object-authoring.md (Plan 02 — THIS IS THE AUTHORING CONTRACT; follow it exactly, especially §8 "Worked example — Withered Sunflower")
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-RESEARCH.md §"Pattern 1" (Aseprite export), §"Pitfall 3" (.ase not in public)
    - lib/design/tokens.ts (Meadow palette — petal yellow may be a NEW token added to tokens.ts during this task)
  </read_first>
  <action>
    **Deliverable:** Two 48×48 PNGs of the Phase 2 Sunflower encounter — withered (before) and revived (after) — sharing the same silhouette bounding box + same base alignment (stem root at local coords 24, 47).

    **What to produce:**
    - **Source:** `design/aseprite/encounter_meadow_sunflower.ase`, 48×48 canvas, 2 frames (or 2 layers toggled per export — tagged `withered` and `revived` if using frames).

    - **Withered state:** drooping head, grey-brown stem (`palette.meadow_grass_shadow` `#42744b` desaturated, or add `palette.sunflower_withered_stem` token), no petals or a few dried-grey petals, outline `palette.ink_900`. Reads "this plant needs help."

    - **Revived state:** upright stem, bright yellow petals radiating from centre, healthy green stem (`palette.meadow_grass_mid` #6fa872 or a new `palette.sunflower_stem` if the stem green is distinct), brown seeded centre, optional celebratory detail (a tiny bee sprite). Reads "solved!"

    - **Silhouette rule:** both states fit the SAME 48×48 cell with the stem root at local (24, 47). Petals extend upward from around (24, 24) to (24, 8). Phaser swaps texture in-place — if silhouettes shift, the hit area drifts (per Plan 02 §3).

    - **Palette discipline:** every colour maps to a token in `lib/design/tokens.ts`. Petal yellow is a NEW token — add it:
      ```typescript
      sunflower_petal: '#e8c547',   // or the exact hex you paint — role-named
      sunflower_center: '#5a3a22',  // seeded centre brown
      ```
      (exact hex values your call; hex MUST be byte-exact written into tokens.ts)

    - **Export:** Use `--split-tags` workflow OR two separate Aseprite exports. Target files:
      - `public/sprites/encounter_meadow_sunflower_withered.png` (48×48)
      - `public/sprites/encounter_meadow_sunflower_revived.png` (48×48)

    - **Commit:** `git add design/aseprite/encounter_meadow_sunflower.ase public/sprites/encounter_meadow_sunflower_*.png lib/design/tokens.ts`

    **Verification before signalling complete:**
    - [ ] `file public/sprites/encounter_meadow_sunflower_withered.png` reports `48 x 48`.
    - [ ] `file public/sprites/encounter_meadow_sunflower_revived.png` reports `48 x 48`.
    - [ ] `test -f design/aseprite/encounter_meadow_sunflower.ase` and size > 500 bytes.
    - [ ] Any new palette hues (petal yellow, centre brown, etc.) are committed in `lib/design/tokens.ts` with role-based names.
    - [ ] `pnpm typecheck` still passes after tokens.ts edits.
    - [ ] Visual QA: place the two PNGs side-by-side in an image viewer — same silhouette bounds, same base alignment.
  </action>
  <verify>
    <automated>test -f public/sprites/encounter_meadow_sunflower_withered.png &amp;&amp; test -f public/sprites/encounter_meadow_sunflower_revived.png &amp;&amp; test -f design/aseprite/encounter_meadow_sunflower.ase &amp;&amp; (file public/sprites/encounter_meadow_sunflower_withered.png | grep -qE '48 x 48|48x48') &amp;&amp; (file public/sprites/encounter_meadow_sunflower_revived.png | grep -qE '48 x 48|48x48') &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test -f public/sprites/encounter_meadow_sunflower_withered.png` exits 0 and file size > 300 bytes
    - `test -f public/sprites/encounter_meadow_sunflower_revived.png` exits 0 and file size > 300 bytes
    - `file public/sprites/encounter_meadow_sunflower_withered.png` reports `48 x 48`
    - `file public/sprites/encounter_meadow_sunflower_revived.png` reports `48 x 48`
    - `test -f design/aseprite/encounter_meadow_sunflower.ase` exits 0
    - `pnpm typecheck` passes after any tokens.ts additions
    - Visual QA: silhouettes match + base alignment consistent (manual)
  </acceptance_criteria>
  <done>
    Sunflower before/after production PNGs committed following the authoring spec (Plan 02). Phase 2 can now wire the Describe encounter — the Sunflower assets are ready. Plan 04 loads both textures in `BootScene.preload()`.
  </done>
  <resume-signal>Type "sunflower-delivered" when both states + .ase + any new tokens are committed, or describe blockers.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: User delivers design/style-guide.png — the §16 visual style guide artifact</name>
  <files>
    design/style-guide.png
  </files>
  <read_first>
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-CONTEXT.md §decisions (D-13 single PNG style guide)
    - docs/cluu-v1-design.md §16 (Next deliverables reference — "single image with colour palette, Cluu reference sheet, tile samples, UI components")
    - lib/design/tokens.ts (every palette token must be visible on the guide)
    - All delivered sprites from Tasks 1–3 (the style guide includes them as reference tiles)
  </read_first>
  <action>
    **Deliverable:** A single PNG image at `design/style-guide.png` that documents the Cluu v1 visual identity per design-doc §16.

    **What to include** (in clearly labelled regions — typographic layout, not pixel-art):
    1. **Palette strip (top)**: every token in `lib/design/tokens.ts` as a colour swatch + token name + hex value. Grouped: "Cross-biome cores" | "Ink scale" | "Meadow biome" | "Sunflower (encounter-added)" | any other biome tokens if added.
    2. **Cluu reference sheet (middle)**: the Content pose at 2× and 4× display size (96×96 and 192×192 visually — zoomed for readability), annotated with which 48×48 silhouette region maps to which cosmetic slot (head / body_pattern / back / eyes) per D-13. Small frames-of-idle-animation row showing breath → blink → head_turn keyframes.
    3. **Tile samples (middle-right)**: the grass tile shown at 4× zoom, plus a 4×4 grid of the tile repeated showing the tileable seam-free behaviour.
    4. **Withered Sunflower before/after (lower middle)**: both states side-by-side at 2× and 4× zoom, with a callout arrow labelled "texture swap on encounter-resolve".
    5. **Encounter-object template (bottom)**: an empty 48×48 grid with guide lines showing stem-root alignment (24, 47), the bounding-box rule, and the palette-discipline callout.
    6. **Footer**: "Cluu v1 · Visual Style Guide · Phase 01.1 · [date]"

    **Dimensions:** Anywhere in the range 1200×1600 to 2400×3200 px (tall portrait — this is a reference doc, not a share card). PNG format, no JPG — keep crisp text.

    **Authoring tool:** User's choice — Aseprite (with text layers), Figma export, or any pixel/vector tool. The artifact must be a PNG at the end.

    **Palette discipline:** every swatch colour on the guide MUST match `lib/design/tokens.ts` byte-exact. If a hex in the guide disagrees with a hex in tokens.ts, one is wrong — fix tokens.ts and re-export, OR re-sample the guide from tokens.ts as the source of truth.

    **Commit:** `git add design/style-guide.png`

    **Verification before signalling complete:**
    - [ ] `test -f design/style-guide.png` and file size > 50 KB (rich image, not a placeholder).
    - [ ] Visual QA: every token in `lib/design/tokens.ts` appears as a swatch on the guide (spot-check 5 random tokens).
    - [ ] Cluu reference shows at 2× zoom at minimum so silhouette reads clearly.
    - [ ] Sunflower before/after is visible.
    - [ ] File dimensions at least 1200×1600.
  </action>
  <verify>
    <automated>test -f design/style-guide.png &amp;&amp; [ $(stat -f%z design/style-guide.png 2>/dev/null || stat -c%s design/style-guide.png) -gt 50000 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f design/style-guide.png` exits 0
    - File size > 50 KB (real image, not a stub)
    - Visual QA self-confirmed: palette strip shows all tokens.ts entries; Cluu reference present at 2×+ zoom; Sunflower before/after present; encounter-object template present; footer dates the artifact (manual)
    - PNG dimensions ≥ 1200×1600 (verifiable via `file design/style-guide.png`)
  </acceptance_criteria>
  <done>
    `design/style-guide.png` is the single artifact satisfying design-doc §16. It is the visual source-of-truth the wider project references when someone asks "what does Cluu look like?" or "what's the Meadow palette?"
  </done>
  <resume-signal>Type "style-guide-delivered" when the PNG is committed, or describe blockers.</resume-signal>
</task>

</tasks>

<threat_model>
**Input surfaces:** none (static image + binary source files committed to repo)
**Data surfaces:** PNG pixel data, Aseprite binary source files — all content-addressed static assets
**Threats:** None identified directly. Theoretical: a malicious contributor could commit a PNG with an embedded EXIF exploit targeting an image library, but Cluu's pipeline only uses standard Node.js fs + Phaser's built-in loader (no server-side image processing). Risk is negligible given solo-dev + committed-to-repo workflow.
**Mitigations:** Commit from trusted local machine only. Rely on Phaser's standard loader (no custom parsers). Do not run arbitrary image-manipulation scripts on committed PNGs.
**Block-on severity:** high (per default `security_enforcement`) — no applicable threats in this phase; mitigation stance is "standard tooling + no server-side processing."
</threat_model>

<verification>
- All 5 PNGs + 1 JSON + 3 .ase files exist at their specified paths
- No .ase files in `public/` (Pitfall 3)
- `cluu_base.png` placeholder deleted (rename to `cluu_content`)
- `cluu_content.json` has `meta.frameTags` with names `breath`, `blink`, `head_turn` (Pitfall 1)
- All PNGs have correct dimensions (`file` output matches spec)
- `pnpm typecheck` passes (any new palette tokens are valid TS)
- Visual QA confirms production art matches D-14/D-15/D-09 intents
</verification>

<success_criteria>
- Plan 04 can `this.load.aseprite('cluu_content', ...)` and `this.anims.createFromAseprite('cluu_content')` creates ≥ 3 animations
- Plan 04 can `this.load.image('encounter_meadow_sunflower_withered'|'_revived', ...)` without errors
- Plan 05 palette audit (when run) finds no new raw-hex leakage from these tasks (all new hues landed in tokens.ts)
- `design/style-guide.png` is the §16 deliverable — readable, comprehensive, committed
- `cluu_base.png` placeholder is deleted; the key rename is "done at the art layer" so Plan 04's code rename is mechanical
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-visual-style-cluu-feel/01.1-03-SUMMARY.md` with: full list of delivered files + sizes, `file` output for every PNG, `jq` output for cluu_content.json tags, list of any new palette tokens added to lib/design/tokens.ts, confirmation of `pnpm typecheck` pass, and visual-QA checklist signed off. Capture a note that Plan 04 can now proceed.
</output>
