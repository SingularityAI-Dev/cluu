---
phase: 01.1-visual-style-cluu-feel
plan: 05
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - tests/design/palette-audit.test.ts
  - tests/design/sprite-fixtures.test.ts
autonomous: true
requirements: []
tags: [testing, audit, verification, exit-gate]

must_haves:
  truths:
    - "Running pnpm test includes the palette-audit test and it passes"
    - "palette-audit.test.ts walks every .ts/.tsx/.css file under the repo (excluding node_modules, .next, coverage, .dot-dirs)"
    - "Audit regex rejects both '#rrggbb' (and short '#rgb') AND '0xrrggbb' forms — Pitfall 5 coverage"
    - "Allowlist contains exactly these entries: lib/design/tokens.ts + the 7 Phase 1 deferred overlay files (app/globals.css, app/global-error.tsx, app/page.tsx, app/auth/signin/page.tsx, app/play/CanvasSkeleton.tsx, ui/ConsentBanner.tsx, ui/SettingsMenu.tsx)"
    - "sprite-fixtures.test.ts verifies every Plan 03 artifact exists at its expected path with a non-trivial byte size"
    - "sprite-fixtures.test.ts verifies cluu_content.json has meta.frameTags with at least the 'breath' tag"
    - "All Phase 1 tests continue to pass"
  artifacts:
    - path: "tests/design/palette-audit.test.ts"
      provides: "Vitest test asserting no raw hex or 0x colour literal exists outside lib/design/tokens.ts and the Phase 1 deferred-overlay allowlist"
    - path: "tests/design/sprite-fixtures.test.ts"
      provides: "Vitest test asserting all Phase 01.1 deliverables exist at expected paths, cluu_content.json has breath frameTag"
  key_links:
    - from: "tests/design/palette-audit.test.ts"
      to: "lib/design/tokens.ts (Plan 01)"
      via: "ALLOWLIST contains 'lib/design/tokens.ts'"
    - from: "tests/design/sprite-fixtures.test.ts"
      to: "public/sprites/cluu_content.png + .json (Plan 03)"
      via: "existsSync + JSON.parse meta.frameTags"
---

<objective>
Ship the two Vitest tests that enforce the Phase 01.1 exit gate (D-22 + D-23): (1) a palette-audit test that fails the build if any raw hex or `0x` colour literal appears outside `lib/design/tokens.ts` or the 7 Phase-1-deferred overlay files, and (2) a sprite-fixtures test that asserts every art deliverable from Plan 03 is present at its expected path with the expected metadata.

Purpose: Without these tests, the "no raw hex outside tokens.ts" rule (D-22) relies on human memory, and D-23's exit-gate checklist stays manual. With them, the next developer to add a scene or an encounter cannot accidentally bypass the palette system — the build fails loudly.

Output: 2 Vitest files. Runs in Wave 3 parallel to Plan 04 (no file overlap — Plan 04 touches game/**, Plan 05 touches tests/**).
</objective>

<execution_context>
@/Users/rainierpotgieter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainierpotgieter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-visual-style-cluu-feel/01.1-CONTEXT.md
@.planning/phases/01.1-visual-style-cluu-feel/01.1-RESEARCH.md
@lib/design/tokens.ts
@docs/encounter-object-authoring.md

<interfaces>
Vitest 4.1 is pinned in package.json (verified in RESEARCH.md). Test files run via `pnpm test`.

Phase 1 deferred-overlay files that the palette audit MUST allowlist (per CONTEXT D-05 — React overlay restyle is Phase 5; these files keep their inline hex for now):
- app/globals.css (colour #2d2a26, background #faf8f3, anchor #2d6a4f)
- app/global-error.tsx (colour #666, background #2d6a4f, colour #fff)
- app/page.tsx (colour #666)
- app/auth/signin/page.tsx (colours #555, #777, border #d9d6ce, background #fff, cta #2d6a4f/#fff)
- app/play/CanvasSkeleton.tsx (background #e8f4d7, colour #4a5240)
- ui/ConsentBanner.tsx (border #2d2a26, background #faf8f3, colour #2d2a26, cta #fff)
- ui/SettingsMenu.tsx (border #d9d6ce, background #fff)

Plus tokens.ts itself (the single valid home for raw hex).

Plan 03 deliverables (paths to assert-exist in sprite-fixtures.test.ts):
- public/sprites/cluu_content.png, .json
- public/sprites/grass_32.png
- public/sprites/encounter_meadow_sunflower_withered.png
- public/sprites/encounter_meadow_sunflower_revived.png
- design/aseprite/cluu_content.ase
- design/aseprite/meadow_grass.ase
- design/aseprite/encounter_meadow_sunflower.ase
- design/style-guide.png
- docs/encounter-object-authoring.md (Plan 02)
- lib/design/tokens.ts (Plan 01)

RESEARCH §"Example 3: Vitest palette audit (D-22 implementation)" — reference implementation.
RESEARCH §"Example 4: Sprite dimension check" — reference implementation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/design/palette-audit.test.ts — D-22 raw-hex guard</name>
  <files>tests/design/palette-audit.test.ts</files>
  <read_first>
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-RESEARCH.md section "Example 3: Vitest palette audit" (reference implementation)
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-CONTEXT.md (D-05 defers React overlay restyle, D-22 audit rule)
    - lib/design/tokens.ts (the one file that is allowed to contain raw hex)
    - All 7 Phase 1 overlay files listed in interfaces (these go in the ALLOWLIST)
  </read_first>
  <action>
Create `tests/design/palette-audit.test.ts` as a Vitest suite. Implementation follows RESEARCH §Example 3 with these exact specifics:

1. Imports: `describe`, `it`, `expect` from `vitest`; `readdirSync`, `readFileSync`, `statSync` from `node:fs`; `join`, `relative` from `node:path`.

2. Constants at top of file:
   - `const ROOT = process.cwd();`
   - `const ALLOWLIST = new Set([...])` — exactly 8 entries, using forward-slash paths relative to repo root:
     - `'lib/design/tokens.ts'`
     - `'app/globals.css'`
     - `'app/global-error.tsx'`
     - `'app/page.tsx'`
     - `'app/auth/signin/page.tsx'`
     - `'app/play/CanvasSkeleton.tsx'`
     - `'ui/ConsentBanner.tsx'`
     - `'ui/SettingsMenu.tsx'`
   - Each allowlist entry has a trailing `// D-05 defer — Phase 5 overlay restyle sweeps this` comment on its own line for future maintainers.
   - `const EXTS = new Set(['.ts', '.tsx', '.css']);`
   - `const HEX = /#[0-9a-fA-F]{3,8}\b|\b0x[0-9a-fA-F]{3,8}\b/;`  (covers both forms; Pitfall 5)
   - `const SKIP_DIRS = new Set(['node_modules', '.next', 'coverage', '.git', '.planning', 'design', 'public', 'supabase', 'opensrc', '.claude']);` — directories the walker never descends into.

3. Recursive `walk(dir: string, out: string[] = []): string[]` function:
   - Iterates `readdirSync(dir)`.
   - Skips any entry whose name starts with `.` OR is in `SKIP_DIRS`.
   - Recurses into directories.
   - Pushes files whose extension is in EXTS.
   - Returns the accumulated paths.

4. Test block:
   - `describe('palette audit (D-22)', () => { ... })`
   - Single `it('no raw hex or 0x colour literals outside tokens.ts', () => { ... })`
   - Body: walk from ROOT; collect offenders as `${relPath}:${lineNumber}  ${lineText.trim()}`; skip allowlisted files; skip lines whose TRIM starts with `//` (ignore single-line comments — pragmatic); `expect(offenders).toHaveLength(0)` with a descriptive error message listing every offender.

5. Add a comment block at the top of the file:
   - "Phase 01.1 D-22 — no raw hex or 0x colour literals outside lib/design/tokens.ts."
   - "ALLOWLIST covers Phase 1 React overlays deferred per D-05 to Phase 5 launch hardening."
   - "To add a new colour: add to lib/design/tokens.ts, reference by name."

Do NOT:
- Do NOT include files from `node_modules`, `.next`, `coverage`, `.planning`, `design/`, `public/`, `supabase/migrations`, `opensrc/`, `.claude/` — SKIP_DIRS covers them.
- Do NOT include `.json` in EXTS (the cluu_content.json intentionally contains hex-like pixel data; it's Aseprite output, not colour config).
- Do NOT include `.md` in EXTS (docs may reference colour tokens but not hex directly; authoring spec in Plan 02 intentionally avoids raw hex so a future strengthening is possible, but isn't required this phase).
- Do NOT use async I/O — sync fs is faster for this workload and Vitest's default environment handles it.
- Do NOT add a second test case for 0x integers only — the regex already covers both forms.
- Do NOT exempt comments that contain hex for documentation purposes from within the AUDIT — if a .ts file comment reads `// was #e8f4d7`, the test catches it. Rationale: "comment prose paraphrases the banned literal" is the STATE.md pattern from Phase 1 D-17 grep gate. If a legitimate doc-comment needs to reference a hex, use the token name (`palette.meadow_hint`) instead.
  </action>
  <verify>
    <automated>pnpm test -- tests/design/palette-audit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/design/palette-audit.test.ts` exits 0
    - `grep -q "D-22" tests/design/palette-audit.test.ts` exits 0 (traceability comment present)
    - `grep -q "lib/design/tokens.ts" tests/design/palette-audit.test.ts` exits 0 (allowlist anchor)
    - `grep -q "app/globals.css" tests/design/palette-audit.test.ts` exits 0 (overlay allowlist)
    - `grep -q "app/global-error.tsx" tests/design/palette-audit.test.ts` exits 0
    - `grep -q "ui/ConsentBanner.tsx" tests/design/palette-audit.test.ts` exits 0
    - `grep -q "ui/SettingsMenu.tsx" tests/design/palette-audit.test.ts` exits 0
    - `grep -q "app/auth/signin/page.tsx" tests/design/palette-audit.test.ts` exits 0
    - `grep -q "app/play/CanvasSkeleton.tsx" tests/design/palette-audit.test.ts` exits 0
    - `grep -q "app/page.tsx" tests/design/palette-audit.test.ts` exits 0
    - `grep -qE "0x\\[0-9a-fA-F\\]" tests/design/palette-audit.test.ts` exits 0 (Pitfall 5 — 0x form in regex)
    - `grep -qE "#\\[0-9a-fA-F\\]" tests/design/palette-audit.test.ts` exits 0 (# form in regex)
    - `pnpm test -- tests/design/palette-audit.test.ts` exits 0 (test PASSES — requires Plan 04 to be complete so game/config.ts + MeadowScene.ts are tokens-clean)
  </acceptance_criteria>
  <done>
    The palette-audit test passes. Future commits that introduce a raw hex outside tokens.ts (or the 7 allowlisted overlay files) will fail `pnpm test` — the Plan 05 safety net is live.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tests/design/sprite-fixtures.test.ts — D-23 exit-gate existence + metadata check</name>
  <files>tests/design/sprite-fixtures.test.ts</files>
  <read_first>
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-RESEARCH.md section "Example 4: Sprite dimension check" (reference implementation)
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-CONTEXT.md (D-23 exit gate: style guide + tokens + sprites + Sunflower + spec doc)
    - All Plan 03 deliverable paths (listed in interfaces)
  </read_first>
  <action>
Create `tests/design/sprite-fixtures.test.ts` as a Vitest suite. Implementation follows RESEARCH §Example 4 with these exact deliverables:

1. Imports: `describe`, `it`, `expect` from `vitest`; `existsSync`, `readFileSync` from `node:fs`.

2. REQUIRED fixture list — exactly these 10 entries, each `{ path, minBytes }`:
   - `{ path: 'public/sprites/cluu_content.png', minBytes: 1000 }`
   - `{ path: 'public/sprites/cluu_content.json', minBytes: 500 }`
   - `{ path: 'public/sprites/grass_32.png', minBytes: 200 }`
   - `{ path: 'public/sprites/encounter_meadow_sunflower_withered.png', minBytes: 300 }`
   - `{ path: 'public/sprites/encounter_meadow_sunflower_revived.png', minBytes: 300 }`
   - `{ path: 'design/aseprite/cluu_content.ase', minBytes: 500 }`
   - `{ path: 'design/aseprite/meadow_grass.ase', minBytes: 500 }`
   - `{ path: 'design/aseprite/encounter_meadow_sunflower.ase', minBytes: 500 }`
   - `{ path: 'design/style-guide.png', minBytes: 50000 }`
   - `{ path: 'docs/encounter-object-authoring.md', minBytes: 1000 }`
   (Plan 01's `lib/design/tokens.ts` is verified implicitly by TypeScript compilation + the palette-audit test importing nothing from it directly. Omitting it from this list keeps concerns separated.)

3. Test block:
   - `describe('phase 01.1 exit-gate fixtures (D-23)', () => { ... })`
   - A for-loop over REQUIRED emitting one `it()` per fixture: `it(`${path} exists and is non-trivial`, () => { expect(existsSync(path)).toBe(true); expect(readFileSync(path).byteLength).toBeGreaterThan(minBytes); })`
   - Additionally, one dedicated `it('cluu_content.json has breath frameTag (Pitfall 1)', () => { ... })`:
     - `const j = JSON.parse(readFileSync('public/sprites/cluu_content.json', 'utf8'));`
     - `const tags: string[] = (j.meta?.frameTags ?? []).map((t: { name: string }) => t.name);`
     - `expect(tags, \`expected breath tag; got [${tags.join(',')}]\`).toContain('breath');`
     - Optional extra asserts on 'blink' and 'head_turn' (if Plan 03 tagged them all — keep the assertion lenient to 'breath' as the must-have since breath is the only anim that plays automatically; blink/head_turn are nice-to-have per D-16).

4. Add a top-of-file comment block:
   - "Phase 01.1 D-23 exit-gate fixtures."
   - "Runs after Plan 03 delivers art assets; failing this test means a required deliverable is missing or empty."
   - "This is an existence + non-trivial-size check, not a visual regression test. Visual QA is manual against design/style-guide.png."

Do NOT:
- Do NOT verify PNG dimensions automatically — ImageMagick is not available on CI (RESEARCH §Environment Availability). Aseprite's Packed layout can reshuffle dimensions so a strict 48×48 check would false-fail. Plan 03's human-task acceptance criteria cover dimension checks via the `file` command manually.
- Do NOT require all three of breath/blink/head_turn to be present — 'breath' is the only must-have (the baseline loop). The other two being missing only degrades UX, doesn't break the game.
- Do NOT fail on large files — there's no upper byte bound; the phase's bundle-budget concern is for runtime-served assets under public/, not design/aseprite/ sources (which are never served).
- Do NOT try to parse the PNG files — use byte-size heuristic only.
  </action>
  <verify>
    <automated>pnpm test -- tests/design/sprite-fixtures.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/design/sprite-fixtures.test.ts` exits 0
    - `grep -q "D-23" tests/design/sprite-fixtures.test.ts` exits 0 (traceability comment present)
    - `grep -q "cluu_content.png" tests/design/sprite-fixtures.test.ts` exits 0
    - `grep -q "cluu_content.json" tests/design/sprite-fixtures.test.ts` exits 0
    - `grep -q "encounter_meadow_sunflower_withered" tests/design/sprite-fixtures.test.ts` exits 0
    - `grep -q "encounter_meadow_sunflower_revived" tests/design/sprite-fixtures.test.ts` exits 0
    - `grep -q "design/style-guide.png" tests/design/sprite-fixtures.test.ts` exits 0
    - `grep -q "design/aseprite/cluu_content.ase" tests/design/sprite-fixtures.test.ts` exits 0
    - `grep -q "docs/encounter-object-authoring.md" tests/design/sprite-fixtures.test.ts` exits 0
    - `grep -q "frameTags" tests/design/sprite-fixtures.test.ts` exits 0 (Pitfall 1 check)
    - `grep -q "toContain('breath')" tests/design/sprite-fixtures.test.ts` exits 0
    - `pnpm test -- tests/design/sprite-fixtures.test.ts` exits 0 (test PASSES — requires Plan 03 artifacts to exist + Plan 02 spec doc to exist)
  </acceptance_criteria>
  <done>
    Every D-23 exit-gate artifact is asserted to exist with a non-trivial byte size, and the Aseprite JSON's breath frameTag is verified (Pitfall 1 coverage). The full `pnpm test` run now validates all phase deliverables in one pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full-suite verification sweep — pnpm typecheck + pnpm test + smoke doc</name>
  <files>none — verification pass only, no edits</files>
  <read_first>
    - tests/design/palette-audit.test.ts (Task 1 — runs green)
    - tests/design/sprite-fixtures.test.ts (Task 2 — runs green)
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-CONTEXT.md (D-23 full exit gate)
  </read_first>
  <action>
This task is a no-edit verification pass. Run the following commands in order and confirm each exits 0; capture the outputs for the SUMMARY.md.

1. `pnpm typecheck` — confirms all TS changes from Plan 04 + tokens.ts from Plan 01 are type-sound.
2. `pnpm test` — runs the full Vitest suite. MUST include:
   - palette-audit.test.ts (PASS)
   - sprite-fixtures.test.ts (PASS)
   - All Phase 1 tests (PASS — tests/rls-negative.test.ts, game/entities/Cluu.test.ts with 'cluu_content' rename, lib/ tests, etc.)
3. `pnpm lint` — ensures Biome has no complaints about the new test files + refactored game files.
4. Manual smoke (human-verifiable, NOT automated in this task but recorded for SUMMARY):
   - `pnpm dev`
   - Open http://localhost:3000/play in a browser.
   - Confirm: production Cluu sprite renders (not red-square), breath animation visible, anchor moves on WASD + tap, Cluu follows anchor, sign-in/sign-out still works (Phase 1 OPS-04 regression-free).

If any automated step fails, do NOT force-pass; surface the failure for debugging. If a human-smoke item fails, note it in the SUMMARY as a blocker requiring user investigation (possibly an asset dimension issue flagged in RESEARCH §Pitfall 4 or §Pitfall 6).

Do NOT:
- Do NOT add new tests in this task — Task 1 + 2 cover the audit + fixtures; further testing (animation playback, visual regression) is out of phase scope.
- Do NOT edit files in this task — it is purely verification.
- Do NOT modify tsconfig or vitest.config — the default paths should resolve tests/design/**.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm test &amp;&amp; pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0
    - `pnpm test` exits 0 AND the output shows BOTH `palette-audit` AND `sprite-fixtures` test files running and passing
    - `pnpm lint` exits 0 (no new Biome issues introduced by Plan 04 or Plan 05 files)
    - Manual smoke checklist recorded in SUMMARY (see Output section) — each line checked or flagged
  </acceptance_criteria>
  <done>
    The full verification pass is green. Phase 01.1 is ready to close against the D-23 exit-gate checklist: (a) style-guide.png exists + reviewed, (b) tokens.ts compiles + imported, (c) sprite PNGs exist, (d) Sunflower before/after exists, (e) authoring spec exists, (f) `pnpm dev` renders production art with no Phase 1 regression.
  </done>
</task>

</tasks>

<threat_model>
**Input surfaces:** none (test files run only in CI/local dev environments)
**Data surfaces:** filesystem reads of source files + fixture PNGs/JSON
**Threats:** the audit test walks the filesystem; a symlink-based path-traversal could in theory redirect the walk outside the repo, but Node's readdirSync + statSync on a trusted repo is not an attack surface — this is developer tooling, not production code.
**Mitigations:** N/A — dev-only; runs against a committed repo.
**Block-on severity:** high (default) — no applicable threats in this phase.
</threat_model>

<verification>
- `pnpm test` exits 0 with both new test files running and passing
- `pnpm typecheck` exits 0
- `pnpm lint` exits 0
- Audit test enumerates raw-hex offenders as error messages if any exist
- Sprite-fixtures test lists missing deliverables as per-fixture failures
- Manual `pnpm dev` smoke confirms no Phase 1 regression + production art renders
</verification>

<success_criteria>
- tests/design/palette-audit.test.ts exists and passes
- tests/design/sprite-fixtures.test.ts exists and passes
- `pnpm test` runs both new tests alongside all Phase 1 tests; every test passes
- Every D-23 exit-gate artifact is asserted by the fixtures test
- Future raw-hex leakage outside tokens.ts (excluding the 7 allowlisted files) will fail CI
- Phase 01.1 is ready to be marked complete in ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-visual-style-cluu-feel/01.1-05-SUMMARY.md` with:
- `pnpm typecheck` exit code + last few lines of output
- `pnpm test` exit code + summary line (e.g., "X tests passed, Y files")
- `pnpm lint` exit code
- Confirmation that tests/design/palette-audit.test.ts + tests/design/sprite-fixtures.test.ts both reported PASS
- Manual smoke checklist:
  - [ ] pnpm dev starts with no errors
  - [ ] /play renders production Cluu sprite (not red square)
  - [ ] Breath animation visible on Cluu
  - [ ] Anchor moves on WASD / tap
  - [ ] Cluu follows anchor (Phase 1 CLUU-01 regression-free)
  - [ ] Sign-in / sign-out still works (Phase 1 OPS-04 regression-free)
- D-23 exit-gate checklist (all 6 items signed off)
- One-line note: "Phase 01.1 closed — ready to update ROADMAP.md marking 01.1 complete and unblocking Phase 2 Walking Skeleton."
</output>
