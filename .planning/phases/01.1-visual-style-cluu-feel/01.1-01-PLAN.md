---
phase: 01.1-visual-style-cluu-feel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/design/tokens.ts
autonomous: true
requirements: []
tags: [design-tokens, palette, typescript]

must_haves:
  truths:
    - "lib/design/tokens.ts compiles under pnpm typecheck"
    - "All Phase 1 inline-style hex values (#faf8f3, #2d2a26, #e8f4d7, #2d6a4f) are preserved verbatim as named tokens — Phase 1 UI does not visually regress when future consumers swap"
    - "D-09 Meadow green triad hex values are present verbatim (#6fa872, #9bc49e, #42744b)"
    - "D-10 Cluu body warm-cream-peach is present (#f4d1b5)"
    - "spacing.tile=32 and spacing.cluu=48 exported (D-12)"
    - "hexToInt() helper accepts '#rrggbb' and returns the Phaser-compatible integer"
  artifacts:
    - path: "lib/design/tokens.ts"
      provides: "Named palette tokens + spacing + radii + hexToInt helper — single source of truth for all colour values in the game"
      exports: ["palette", "spacing", "radii", "hexToInt", "PaletteToken", "PaletteHex"]
      min_lines: 60
  key_links:
    - from: "lib/design/tokens.ts"
      to: "game/config.ts (consumer in Plan 04)"
      via: "import { palette, hexToInt } from '@/lib/design/tokens'"
      pattern: "hexToInt\\(palette\\.meadow_hint\\)"
    - from: "lib/design/tokens.ts"
      to: "tests/design/palette-audit.test.ts (consumer in Plan 05)"
      via: "audit test ALLOWLIST contains 'lib/design/tokens.ts'"
      pattern: "lib/design/tokens\\.ts"
---

<objective>
Create `lib/design/tokens.ts` as the canonical design-token module for Cluu: named palette (5 cross-biome cores + Meadow triad), semantic spacing (tile=32, cluu=48), forward-looking radii, and a `hexToInt()` helper so Phaser scene code stays token-referential. Every Phase 1 inline-style hex value is preserved verbatim as a named export so the deferred React-overlay restyle (D-05) can swap imports in a later phase without a colour shift.

Purpose: Without a single-source-of-truth palette, Plan 04's scene background refactor and Plan 05's palette-audit test have nothing to reference. This plan is Wave 1 because every other plan depends on it — the audit test allowlists this file, and the Phaser config consumes `hexToInt(palette.meadow_hint)` instead of the current literal `'#e8f4d7'`.

Output: One TypeScript file with the palette, spacing, radii, and `hexToInt` helper — ready for Plan 04 (scene consumer) and Plan 05 (audit allowlist anchor).
</objective>

<execution_context>
@/Users/rainierpotgieter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rainierpotgieter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-visual-style-cluu-feel/01.1-CONTEXT.md
@.planning/phases/01.1-visual-style-cluu-feel/01.1-RESEARCH.md

<interfaces>
<!-- Current inline hex literals that MUST be preserved as named tokens
     (scraped from Phase 1 code via `grep '#[0-9a-fA-F]\{3,6\}'` on 2026-04-21).
     The audit test in Plan 05 will allowlist these files; the tokens must
     nevertheless carry the same hex values so Phase 5's overlay-restyle sweep
     is a pure `import`-replacement with zero visual diff. -->

From app/globals.css:
  body color: `#2d2a26`  → ink_900
  body background: `#faf8f3`  → ui_paper
  a color: `#2d6a4f`  → accent_green

From game/config.ts:
  backgroundColor: `#e8f4d7`  → meadow_hint (camera/scene bg)

From app/play/CanvasSkeleton.tsx:
  background: `#e8f4d7`, color: `#4a5240`  → meadow_hint, ink_700 (new token for the skeleton text)

From app/global-error.tsx / app/auth/signin/page.tsx / ui/ConsentBanner.tsx / ui/SettingsMenu.tsx / app/page.tsx:
  `#666`, `#555`, `#777`, `#fff`, `#d9d6ce`  → ink_500, ink_600, ink_400, white, stone_200 (gray scale + chrome)

<!-- D-09 (locked): Meadow green triad -->
  meadow_grass_mid:        `#6fa872`
  meadow_grass_highlight:  `#9bc49e`
  meadow_grass_shadow:     `#42744b`

<!-- D-10 (locked family): Cluu warm cream/peach body — this exact hex is the starting point; planner can tweak in Plan 03 art iteration, but the family (warm cream/peach) is non-negotiable -->
  cluu_body: `#f4d1b5`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create lib/design/tokens.ts with palette + spacing + radii + hexToInt helper</name>
  <files>lib/design/tokens.ts</files>
  <read_first>
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-CONTEXT.md (locked decisions D-09, D-10, D-11, D-12)
    - .planning/phases/01.1-visual-style-cluu-feel/01.1-RESEARCH.md (§"Pattern 4: Design-token file shape", §"Example 4" audit test structure)
    - app/globals.css (preserve these hex values)
    - game/config.ts (preserve #e8f4d7 as meadow_hint)
    - ui/ConsentBanner.tsx, ui/SettingsMenu.tsx, app/page.tsx, app/auth/signin/page.tsx, app/play/CanvasSkeleton.tsx, app/global-error.tsx (preserve all referenced hex values — per D-05 defer, overlays stay as-is; tokens mirror so Phase 5 sweep is lossless)
  </read_first>
  <behavior>
    - `palette` exports named tokens for every hex currently used in Phase 1 code + the D-09 Meadow triad + D-10 Cluu body family.
    - `palette` is `as const` so `typeof palette` yields a string-literal discriminated union.
    - `PaletteToken = keyof typeof palette`; `PaletteHex = (typeof palette)[PaletteToken]`.
    - `spacing = { tile: 32, cluu: 48, encounter: 48 } as const` (D-12 + D-21 encounter default).
    - `radii = { sm: 4, md: 8, lg: 16 } as const` (D-12 forward-looking for Phase 5 UI polish).
    - `hexToInt(hex: ` + "`#${string}`" + `): number` — strips the leading `#` and `parseInt(..., 16)`. Handles both `#rrggbb` and `#rgb` shorthand (expand to 6-char before parse).
    - File has a top-of-file comment asserting the "no raw hex outside this file" rule so future readers see it before the audit test fires.
    - Every token name is role-based (`meadow_grass_shadow`), not pigment-based (`green900`) — per RESEARCH §"Anti-Patterns".
  </behavior>
  <action>
    Create `lib/design/tokens.ts` with:

    1. Header comment block:
       - "Canonical design tokens for Cluu" title
       - "RULE: No raw hex codes outside this file. Enforced by tests/design/palette-audit.test.ts (Plan 05)."
       - Cite D-11 (palette shape), D-12 (spacing), D-22 (audit rule)

    2. `export const palette = { ... } as const;` with EXACTLY these keys and values:
       ```
       // --- Cross-biome cores (5) — preserves Phase 1 inline palette ---
       ui_paper: '#faf8f3',         // body background (globals.css:7)
       ink_900: '#2d2a26',          // body text (globals.css:6)
       accent_green: '#2d6a4f',     // anchor + primary CTA (globals.css:15, signin/page.tsx:46)
       meadow_hint: '#e8f4d7',      // Phaser scene background (config.ts:14)
       cluu_body: '#f4d1b5',        // D-10 warm cream/peach

       // --- Ink scale (preserved from Phase 1 inline styles — D-05 defer) ---
       ink_700: '#4a5240',          // CanvasSkeleton text (play/CanvasSkeleton.tsx:15)
       ink_600: '#555555',          // signin subtitle (signin/page.tsx:17)
       ink_500: '#666666',          // error copy + landing text (global-error.tsx:23, page.tsx:14)
       ink_400: '#777777',          // signin helper copy (signin/page.tsx:20)
       white: '#ffffff',            // CTA text + cards (global-error.tsx:30, signin/page.tsx:35, ConsentBanner.tsx:50, SettingsMenu.tsx:60,80)
       stone_200: '#d9d6ce',        // input + menu borders (signin/page.tsx:34, SettingsMenu.tsx:59,82)

       // --- Meadow biome (D-09 locked triad + extensions allowed) ---
       meadow_grass_mid: '#6fa872',
       meadow_grass_highlight: '#9bc49e',
       meadow_grass_shadow: '#42744b',
       ```

    3. `export type PaletteToken = keyof typeof palette;` and `export type PaletteHex = (typeof palette)[PaletteToken];`

    4. `export const spacing = { tile: 32, cluu: 48, encounter: 48 } as const;` with JSDoc cross-referencing D-10 (tile), Phase 1 D-13 (cluu 48×48), D-21 (encounter-object default).

    5. `export const radii = { sm: 4, md: 8, lg: 16 } as const;` with JSDoc "Forward-looking for Phase 5 UI polish (D-12). No Phase 01.1 consumer."

    6. `export function hexToInt(hex: ` + "`#${string}`" + `): number` — implementation:
       ```
       const raw = hex.slice(1);
       const full = raw.length === 3
         ? raw.split('').map(c => c + c).join('')
         : raw;
       return parseInt(full, 16);
       ```
       Add a JSDoc example: `hexToInt(palette.meadow_hint) // => 0xe8f4d7 = 15266519`.

    Import path for consumers: `@/lib/design/tokens` (matches the `@/*` alias in tsconfig; verify `tsconfig.json` has `"baseUrl"` + `"paths"` mapping before committing — if not present, use relative imports in Plan 04 instead).

    Do NOT:
    - Do NOT add Tailwind config output (no Tailwind in this project).
    - Do NOT write a tokens→CSS generator (D-22 defer; Plan 05 allowlists .css files).
    - Do NOT add `satisfies` without a schema — unnecessary complexity per RESEARCH §"Pattern 4".
    - Do NOT use an `enum` — use `as const` per TypeScript handbook + RESEARCH §"Why as const + object".
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; grep -q "cluu_body: '#f4d1b5'" lib/design/tokens.ts &amp;&amp; grep -q "meadow_grass_mid: '#6fa872'" lib/design/tokens.ts &amp;&amp; grep -q "meadow_grass_highlight: '#9bc49e'" lib/design/tokens.ts &amp;&amp; grep -q "meadow_grass_shadow: '#42744b'" lib/design/tokens.ts &amp;&amp; grep -q "ui_paper: '#faf8f3'" lib/design/tokens.ts &amp;&amp; grep -q "ink_900: '#2d2a26'" lib/design/tokens.ts &amp;&amp; grep -q "meadow_hint: '#e8f4d7'" lib/design/tokens.ts &amp;&amp; grep -q "accent_green: '#2d6a4f'" lib/design/tokens.ts &amp;&amp; grep -q "tile: 32" lib/design/tokens.ts &amp;&amp; grep -q "cluu: 48" lib/design/tokens.ts &amp;&amp; grep -q "export function hexToInt" lib/design/tokens.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/design/tokens.ts` exists and `pnpm typecheck` exits 0
    - `grep -c "as const" lib/design/tokens.ts` returns ≥ 3 (palette, spacing, radii)
    - `grep -q "cluu_body: '#f4d1b5'" lib/design/tokens.ts` exits 0 (D-10 family)
    - `grep -q "meadow_grass_mid: '#6fa872'" lib/design/tokens.ts` exits 0 (D-09)
    - `grep -q "meadow_grass_highlight: '#9bc49e'" lib/design/tokens.ts` exits 0 (D-09)
    - `grep -q "meadow_grass_shadow: '#42744b'" lib/design/tokens.ts` exits 0 (D-09)
    - `grep -qE "meadow_hint: '#e8f4d7'" lib/design/tokens.ts` exits 0 (game/config.ts preservation)
    - `grep -qE "ui_paper: '#faf8f3'" lib/design/tokens.ts` exits 0 (globals.css preservation)
    - `grep -qE "ink_900: '#2d2a26'" lib/design/tokens.ts` exits 0 (globals.css preservation)
    - `grep -qE "accent_green: '#2d6a4f'" lib/design/tokens.ts` exits 0 (globals.css preservation)
    - `grep -qE "^export const spacing" lib/design/tokens.ts` exits 0 and `tile: 32` + `cluu: 48` appear (D-12)
    - `grep -qE "^export const radii" lib/design/tokens.ts` exits 0 (D-12)
    - `grep -qE "^export function hexToInt" lib/design/tokens.ts` exits 0
    - `grep -qE "^export type PaletteToken" lib/design/tokens.ts` exits 0
    - File does NOT import from `phaser` (tokens must stay framework-agnostic — Phaser config consumer imports via hexToInt in Plan 04)
    - No external deps added to `package.json`
  </acceptance_criteria>
  <done>
    `lib/design/tokens.ts` is a pure TypeScript constants file exporting `palette`, `spacing`, `radii`, `hexToInt`, and the two type aliases. It contains every Phase 1 inline-style hex as a named token, the D-09 Meadow triad, and the D-10 Cluu body colour. `pnpm typecheck` passes. Plan 04 can now `import { palette, hexToInt } from '@/lib/design/tokens'` and Plan 05's audit test can allowlist this exact path.
  </done>
</task>

</tasks>

<threat_model>
**Input surfaces:** none (pure static TypeScript constants file, no user input, no network, no auth)
**Data surfaces:** compile-time palette + spacing constants; no runtime state
**Threats:** none identified — adding named constants cannot introduce an exploitable surface
**Mitigations:** N/A
**Block-on severity:** high (per default `security_enforcement` enabled) — this plan has no threats to block on
</threat_model>

<verification>
- `pnpm typecheck` exits 0 with tokens.ts compiled
- Every locked hex (D-09 triad, D-10 cluu body) is grep-verifiable in the file
- Every Phase 1 inline hex has a preservation token so future Phase 5 sweep is a pure import swap
- File exports are importable via `@/lib/design/tokens` (Plan 04 will verify live)
- No Phase 1 regression: this plan only ADDS a file; no existing consumer changes (Plan 04 handles the game/config.ts swap)
</verification>

<success_criteria>
- lib/design/tokens.ts exists with all 14 required palette keys + spacing + radii + hexToInt
- D-09 Meadow triad hex values are byte-exact
- D-10 Cluu body hex value is byte-exact
- All Phase 1 inline-style hex values have a named token (discoverable via grep)
- `pnpm typecheck` passes
- `hexToInt('#e8f4d7')` evaluates to `0xe8f4d7` (verified via any consumer or ad-hoc test)
- No new dependencies in package.json
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-visual-style-cluu-feel/01.1-01-SUMMARY.md` with: tokens.ts file path, list of every palette key + hex value shipped, confirmation of D-09 + D-10 byte-exact match, typecheck result, and a one-line note that Plans 04 + 05 can now proceed.
</output>
